# 贡献指南

这是一个**个人自用**的工具，开源出来是因为「本地优先 + 可自建部署」的东西本来就该让人看得见。欢迎提 Issue 和 PR，但请注意下面的取舍原则 —— 它们不是随便定的，每一条都对应一个踩过的坑或者被证据推翻过的直觉。

## 提交 Issue 之前

请先确认你的问题**不是**设计上刻意的选择。以下几条看起来像缺陷，但其实是刻意的：

| 现象 | 为什么这样设计 |
|---|---|
| 记一句话时，`交物业费 200` 没被识别成 200 元支出 | 识别规则**保守优先**：宁可让你多点一次归类，也不能把账记错。数字不在开头就不当金额 |
| 认不出来的内容进了收件箱而不是报错 | 「先存下来别丢」优先于「必须归类」。收件箱就是给不确定留的位置 |
| 漏打卡之后没有「连续天数归零」的提示 | 有明确证据表明 streak 的断点惩罚是打卡类功能被放弃的直接触发器。不做惩罚是设计决定 |
| 没有红点、没有催促提醒 | 同上。靠「打开就看到今天」而不是靠焦虑驱动 |
| 复盘页面看不到随心记的原文 | 默认**隔离**。知道会被翻出来看，写的时候就会开始表演。用户显式开启才会进聚合 |
| 记录项只有「纯文字」和「数值」两种方式 | 字段越多录入越慢，录入越慢这个功能就越会死。这两种已覆盖「记录过程」和「观察趋势」 |
| 领域删除后内容回到收件箱而不是被删掉 | 分类可以错，数据不该丢。否则你以后就不敢删、也不敢建新领域了 |

## 提 PR 之前

1. **先读 `docs/开工方案.md`** —— 它是唯一有效的开工依据。数据模型和交互约定都在里面，改动如果和它冲突，请先提 Issue 讨论。
2. **不要引入需要联网才能工作的核心功能。** 核心层必须能在断网、无账号、无任何外部服务的情况下完整工作。这是这个项目的第一原则。
3. **不要给核心功能加 AI 依赖。** 扩展层可以，但必须可一键关闭，且关掉之后功能不减一分。
4. **新功能必须能被「今日视图」或「领域页」吸收。** 如果它两个都进不去，它大概率不该进这个应用。
5. 每新增一个模块，**必须同时提供移除它的办法**。

## 代码约定

- **不在代码里硬编码任何密钥、路径、账号。** 一律走 `.env`（模板见 `.env.example`）。
- 前端用 TypeScript，不开 `any` 逃逸口。
- 后端用 Python，类型注解写全 —— 它同时是 OpenAPI schema 的来源。
- 提交信息用中文或英文都可以，但请写清「改了什么、为什么」。

## 里程碑对应关系

开发顺序是 5 步（见 README 的「路线」），**每一步结束时都必须能真的打开来用**。请按当前所处阶段提交，不要把后面的模块提前混进来 —— 提前混入会导致任何一处设计错误都要到最后才被发现。

## 许可

提交 PR 即表示你同意你的贡献以 [AGPL-3.0](LICENSE) 授权。
