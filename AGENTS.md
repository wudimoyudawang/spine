# AGENTS.md · 改这个仓库的工程标准

> **这份文件管「怎么写」，不管「写什么」。**
>
> | 想知道 | 读 |
> |---|---|
> | 现在做到哪了、下一步做什么、最近踩了什么坑 | [`PROMPT.md`](PROMPT.md) |
> | 产品为什么是这样、哪些决策已经定死 | [`HANDOFF.md`](HANDOFF.md) |
> | 数据模型、后端设计、验收标准 | [`docs/开工方案.md`](docs/开工方案.md) |
> | **改代码时必须遵守什么**（本文件） | `AGENTS.md` |
>
> 面向前来改代码的 AI 助手，也面向人和自己。**动手前通读一遍**，
> 尤其是第 2 节（硬约束）和第 5 节（验证标准）—— 这两节是「交付算不算完成」的定义。
>
> 这些规则不是风格偏好。每一条后面都挂着一次真实事故或一组实测数字，
> 第 9 节列了出处。想推翻某条之前，先去看它是怎么来的。

---

## 0. 三十秒版

1. **数据只有一个来源**：`app/src/stores/db.js`。视图和组件不许自己算业务、不许直接改 `db.xxx`。
2. **行为变更必须是显式的**。重构不许顺手改行为 —— 包括「顺手修脏数据」，那是数据事故。
3. **改完必须跑验证**：`npm test` 是最低要求；动了视图/模板还要 `npm run test:e2e`。
4. **不许靠「我读了代码觉得没问题」交付**。每个结论都要有可复跑的证据。
5. **不许提交构建产物**，不许动 `prototype/`，不许新增需要联网或 AI 才能工作的核心功能。
6. **注释写「为什么」，不写「做了什么」**。这个仓库的注释是决策记录，是资产。

---

## 1. 动手之前

### 1.1 读的顺序

```
AGENTS.md（本文件）→ PROMPT.md（现状与待办）→ HANDOFF.md（产品决策）
→ docs/开工方案.md（数据模型）→ 具体要改的那个模块
```

改视图之前，**先读对应的数据层函数**。这个项目的视图很薄，逻辑都在 `db.js` ——
在视图里「就地解决」的问题，通常数据库里已经有一个正确的入口了。

### 1.2 先跑基线，再动刀

```bash
cd app
npm test          # 对拍 + 组件交互，几秒
```

**改之前跑一遍**。如果改之前就是红的，那是既有问题（先报告，别顺手修）；
如果改完变红，是你改坏了 —— 这两种情况的处理方式完全不同，混在一起会浪费一整轮。

### 1.3 一次只改一件事

性能优化、行为修复、样式去重、架构重构 —— **分开做，分开验**。
混在一批里，对拍报出差异时你分不清是哪一件引起的。

---

## 2. 不可违反的约束

### 2.1 架构三条（来自 README，不接受折中）

| # | 约束 | 为什么 |
|---|---|---|
| 1 | 核心层**不许**依赖网络、账号或任何外部服务 | 本地优先。断网、无账号时必须能完整工作 |
| 2 | 核心功能**不许**依赖 AI | AI 只能长在可一键关闭的扩展层，关掉之后功能不减一分 |
| 3 | 新功能必须能被「今日」或「领域页」吸收 | 两个都进不去，它大概率不该进这个应用 |

### 2.2 数据格式与对外接口

- **localStorage key `spine.state.v2` 不许改**。改了等于所有老用户数据静默丢失。
- **导出文件格式 `fmt: 2` 不许改**。要改就先写迁移，并且**必须先问用户**。
- **`db.js` 的导出只增不减**（除非确认全仓无调用方，并且用户同意）。
  新增参数一律放尾部、给默认值 —— 现有调用方不该因为你的重构而改动。
- **记录项的日期是中文展示串（`RECORD_TYPES[].logs[].d`）**。看着别扭，但它是既有数据格式。

### 2.3 不许提交产物

`.gitignore` 已拦住 `shell/android/**/build/`、`shell/www/`、`app/dist/`、
`.gradle/`、`test/.shots/`。**`git status` 里出现这些就是你的操作越界了**，
不要去 `git add -f`。

仓库里体量大得吓人的那些目录（Android 构建产物）**本来就没被跟踪**，
不要因为「文件很多」就以为历史里塞了产物。

### 2.4 不许改 `prototype/index.html`

它是**行为参照物**，是定稿过的基准，不是一份还能改的代码。

---

## 3. 跨模块契约

这几样东西一旦动了，**全仓都要跟着同步**。改动它们之前在仓库里搜一遍。

### 3.1 数据只有一个来源

```
views/  components/        ← 只读 db 的状态、只调 db 的函数
        ↓
    lib/ui.js              ← 只碰「界面怎么说」（toast / 两段确认）
        ↓
 stores/db.js  stores/seed.js   ← 唯一的数据层，唯一改数据的地方
```

**依赖方向是单向的，不许有反向依赖。** 视图里出现 `db.ITEMS.push(...)`
这种直接改数据、绕开函数入口的写法，就是错的。

### 3.2 `spec` 字符串 —— 行的统一身份

界面上一行用 `spec` 标识，`resolveNode(spec)` 是唯一的解析入口。

| 前缀 | 指什么 | 例 |
|---|---|---|
| `item:` | 待办（住在 `db.ITEMS`） | `item:it3kf2-7` |
| `habit:` | 习惯（住在所属 `domain.habits`） | `habit:h1` |
| `goal:` | 长期目标（住在 `domain.goals`） | `goal:g2` |
| `rt:` | 记录项（`rt:<rtId>` 或某一条 `rt:<rtId>\|<logId>`） | `rt:rt_weight\|9` |
| `money:` | 一笔记账 | `money:lg12` |
| `tlog:` | 今日速记的流水行 | `tlog:tl3` |
| `cat:` | 记账品类 | `cat:餐饮` |
| `rule:` | 自动判断规则 | `rule:ru1` |
| `note:` | 随心记 | `note:n5` |
| `inbox:` | 收件箱一条 | `inbox:in2` |
| `trend:` | 复盘趋势行 | `trend:money` |
| `domain:` | 领域本身 | `domain:d3` |

**新增一类可点的行，就在这里加一行前缀，并让 `resolveNode` 认它。**

### 3.3 `labelOf(node)` —— 取名字的唯一入口

待办用 `title`、习惯和目标用 `t`。**不许在各处自己判断**（`node.title || node.t`）——
漏一处就是一片空白。要显示名字一律走 `labelOf`。

### 3.4 界面用语只有一个出口：`lib/ui.js`

- 提示一律 `toast('...')`，**不许直接写 `uni.showToast`**（现在全仓只有 1 处，就是它的实现）。
- 删除一律走 `confirmDelete(spec)`，**不许直接调 `armDelete`**。
- 「再点一次『确认删』」这类文案提成常量（`CONFIRM_HINT`），**不许在多处各写一份**。

例外：**成功那句各写各的**，这是刻意的 —— 删品类要说「已记的没改」，
删领域要说「内容已回到收件箱」。统一成「已删除」等于把最要紧的半句丢掉。

### 3.5 两段确认是全局唯一的闸门

`delArmed` 是全局状态：**同一时刻全应用只有一处处于「已武装」**。
新增任何破坏性操作（清空、恢复、删规则…）都接这个闸门，不要自己另起一个
`xxxArmed` + 定时器 —— 那会变成两套互相不知道的确认状态。

### 3.6 派生值算一次，挂到行对象上

模板里**不许调用会扫全表的函数**。原来今日页一行里 `streak()` 调 3 次、
`habitDoneOn()` 调 2 次，每次都是一遍全表扫描 + 排序 —— 106 行要 1.2 秒。
现在是数据层算一次、挂到行对象上（`row.streak` / `row.doneToday`），模板直接读。

**判断标准**：模板里出现 `f(r.node.id)` 这种「按 id 查一遍」的调用，就要挪进数据层。

### 3.7 「导入」和「恢复」是两种语义，不许合成一个函数

| 入口 | 语义 | 服务的对象 |
|---|---|---|
| `importSnapshot()` → **`replaceAll()`** | **整体替换**：档案里缺哪一项，本机那一项就清空 | 用户导入的档案 / 剪贴板（一份完整快照） |
| `restore()` | **合并**：档案里有哪一项才覆盖哪一项 | `loadState()` 读 localStorage、`restoreBackup()` 读本机备份 |

**为什么必须分开**：`restore` 的两条来路都是「本机自己的数据」，那里的档案可能是
**老版本写的**、天生缺后加的 key —— 在那里「缺就清空」等于版本升级时把用户数据抹掉。
而导入的档案是一份完整快照，缺什么就是没有；「缺了就保留本机」会让残缺档案
看起来导入成功、实际留下两边的混合数据。

**两者共用 `DATA_KEYS` 这一份清单**（16 项），范围不许分叉 ——
分叉的后果是「导入一份自己刚导出的文件都会丢东西」。
对拍里 `seq.importReplace.selfExport` 专门钉住这一点。

**为什么不加一个布尔开关**：这两种语义的差别是「漏一项就丢数据」级别的，
藏进参数里迟早被误用。宁可两个函数、两段注释写清。

**`UI_KEYS` 两种语义都不整体替换**：那是「我在哪一页」，跟着设备走
（导入时 `importSnapshot` 还会把 `CURRENT` 显式还原回去）。

**`replaceAll` 里「形态非法」和「缺项」是两件事**：缺项 = 清空（那是导入该有的语义）；
形态非法（`ITEMS: null`）在**计算阶段**就降级为空 —— 否则赋值之后的补 id 那一步会抛错，
留下「数据换了、id 没补」的半死状态。整个替换要么全成、要么完全不动。

**破坏性动作要能反悔**：`importSnapshot` 在换之前会 `keepBackupAs('… 导入前')`，
和 `restoreBackup` 的第一步对称。自动备份是「每天第一份」，未必覆盖得住此刻的最新改动。

---

## 4. 代码纪律

### 4.1 性能

| 规则 | 具体做法 |
|---|---|
| **不要在循环里调单条查询** | `nodePath` / `itemById` 这类是按签名 O(n) 的。批量要用预建索引的版本（`flattenTree` 内部就是这么做的） |
| **建索引只建一次** | 一趟遍历里需要 id 表 / 分组表，就在入口建一次往下传，不要每个节点重建 |
| **能一趟走完的不要走多趟** | `weekView` 原来是 7 天各扫一遍，现在按天摊成索引表一趟出 |
| **先判断再付代价** | 「没变化就跳过」的优化必须写在付代价**之前**。`saveState` 原来是先全量序列化再比字符串（45.7ms/2s，约 2.29% CPU 常驻），现在先看变更标记，稳态 0.003ms |
| **量出来再下结论** | 「看起来慢」不算发现。用 `test/digest.cjs` 的数据规模实测，附数字 |

**注意**：本项目的数据规模（几个领域、几百条）下，很多 O(n²) 也感觉不出来 ——
但增长曲线是坏的，用户数据会一直长。**优化时看增长曲线，不看绝对值。**

### 4.2 uni-app 的坑（每次都要查）

| 坑 | 规矩 |
|---|---|
| **`<input>` / `<textarea>` 不写 `maxlength` 默认 140 字，且静默截断** | 每个输入框都要写 `:maxlength="-1"`。「不限制」只能这么表达 |
| `<view>` 上的 `@click` 在部分平台不触发 | 用 `@click.stop` 明确阻止冒泡，别依赖默认行为 |
| **`<style scoped>` 不跨组件** | 组件搬走了，它用到的样式要跟着搬。`.tick` 就是这么从 `today.vue` 搬进 `TreeList.vue` 的 |
| 页面生命周期（`onHide` 等）来自 `@dcloudio/uni-app` | 不要在 `App.vue` 和页面里写两遍同一件事 |
| 别用 emoji 当图标 | 界面上一律用 CSS 或 `PlusIcon.vue` 这类组件 |

### 4.3 样式

- **多个页面逐字相同的规则，提到 `styles/base.scss`** 的「多个页面共用的骨架类」段。
- **近似的规则不要硬合并** —— `.block-h` 有几种变体、`notes.vue` 的 `.delbtn` 少一个
  `margin-left`。合并这些会改掉视觉。**只合并声明体逐字相同的。**
- 颜色一律用 CSS 变量（`var(--line2)` 这类），不要在组件里写死十六进制
  （四象限色是数据不是样式，走 `db.QUAD_COLORS`，那是例外）。

### 4.4 注释

**写「为什么」，不写「做了什么」。** 这个仓库的注释密度很高，那是有意的 ——
它同时承担决策记录的功能。参考现有的写法：

```js
/* 子项继承父项的**领域、日期和四象限** —— 理由同前：它是「这件事的一部分」，
   不是另一个独立承诺。四象限不继承的话，四象限页那一列是平铺的，
   一条子项会跑得离父项很远，看着像两件不相干的事。 */
```

改动别人的代码时，**如果注释说的和代码现在做的不一样了，改注释或改代码，不许留着不一致**。
（`build.cjs` 里就有一条断言因为探针串改过而永远通过 —— 后来修好了。
 这种「看起来在检查、其实什么也没检查」的东西比没有更危险。）

---

## 5. 验证标准

### 5.1 三套各守一条线，不可互相替代

| 套件 | 命令 | 守的线 | 需要浏览器 |
|---|---|---|---|
| **行为等价性对拍** | `npm run test:equiv` | 重构不许改行为 | 否，毫秒级 |
| **组件交互测试** | `npm run test:component` | 编译产物对了 ≠ 运行期对 | 否（jsdom） |
| **端到端 + 截图** | `npm run test:e2e` | 界面上真的能用 | 是（先 build） |

`npm test` = 对拍 + 组件。**端到端不在里面**，因为它需要先构建、起浏览器、慢得多 ——
**动了视图 / 组件 / 样式就必须单独补跑**。

### 5.2 改什么 → 跑什么

| 你改了什么 | 最少要跑 |
|---|---|
| `stores/db.js`（逻辑） | `npm test` +（动了取数结构还要）`test:e2e` |
| 组件（`.vue` 的 script） | `npm test` + `test:component` |
| 视图模板 / 插槽 / 组件结构 | `npm run build:h5` + `test:e2e` |
| **纯样式** | `build:h5` + **改动前后各截一套图逐像素比**（见 5.4） |
| `shell/` | `node shell/build.cjs`（它自带自检） |

### 5.3 对拍的用法（**这条最容易用错**）

`test/fixtures/digest.json` 是金标准。当前输出与它**逐字节比对**。

**差异必须逐条看，看清楚再决定。** 三种情况：

1. **你刻意改的**（新增了导出字段、按注释订正了行为）→ 确认无误后 `npm run test:equiv:update`。
2. **你改坏的** → 修代码，不要 update。
3. **对拍自己错了**（数据造得不全、id 归一化没做）→ 修对拍，再跑。

**绝不允许**：看到红就 `--update`。那等于把安全网拆了。

对拍为什么可信，靠两个设计（改动前先读 `test/README.md`）：
**时钟冻结**（不冻结，金标准明天就失效）和**刻意刁钻的数据**（空父项、孤儿 `parent`、
跨月周边界、**区间重叠**、脏记录）。本项目靠它抓到过一处真实的行为差异。

### 5.4 纯样式改动的验证 = 像素比对

```
build → test:e2e --shots before/  →  改样式  →  build  →  test:e2e --shots after/  →  逐像素比
```

PNG 编码是确定性的：**先试逐字节相等**，不等就解 PNG 逐像素比
（zlib 解压 + 按滤波器还原，不需要装图像库）。

**有差异不代表改坏了。** 页头那句「已保存 HH:MM」每张图都不一样 ——
把差异区域的包围盒打出来、转成 ASCII 看一眼是什么，再下结论。
（本项目 CSS 去重后 10 张图里 8 张有差异，全部是那两个分钟数字，其余逐像素一致。）

### 5.5 不许这么交付

- ❌「我看了代码，逻辑上没问题」—— 跑一下。
- ❌「对拍全绿所以没问题」—— 对拍只覆盖数据层，界面回归要靠端到端。
- ❌「构建通过了」—— 构建通过只说语法对了。
- ❌ 只跑自己新写的那一条 —— **回归网的意义在全绿，不在单绿。**

---

## 6. 什么算「行为变更」

### 6.1 定义

**用户能观察到的任何变化**：屏幕上的文案、数字、顺序、颜色、可点的东西、
存下去的值、导出文件的内容 —— 都算。函数签名不变、返回值多一个字段，也算。

**行为变更必须显式声明**：写进交付说明，标出来，并说清「改前是什么、改后是什么」。
重构的默认假设是「行为完全不变」，任何例外都要单独交代。

### 6.2 不要顺手「修」脏数据

**这条是有血的。** 本项目有一次把打卡记录做成索引，索引顺手跳过了「空日期」的
脏记录（`{key:'h1', date:''}`），而旧实现把它当成一天算进「累计天数」——
于是老档案的「累计 N 天」会当场变小，且用户无从解释。

**规矩**：重构时**行为一致性优先于数据整洁**。
发现脏数据是对的，但改动要单独提出来、单独确认 —— 那不是重构的附带效果。

**另一个方向也要注意**：`importSnapshot` 曾经是「合并」而不是注释和界面说的「整体替换」——
界面一直写着「导入会把本机换成这一份」「覆盖导入」，实现却是「档案里缺的项保留本机值」，
于是导入一份残缺档案会悄悄留下两边的混合数据，而屏幕上显示导入成功了。

**2026-09-23 已按界面说的改对**：导入 = 整体替换（缺项即清空），
`restore` 保持合并语义、只服务于「本机自己的数据」那条路。见 3.7。
教训是通用的：**界面文案和实现不一致时，先判断哪个是对的，再让另一个跟上** ——
这里界面是对的，该改的是实现。

### 6.3 修复与重构分开

- **修复**（改行为）：单独一批，单独验，单独说明。
- **重构**（不改行为）：靠对拍兜着，可以批量做。

两者混在一起时，对拍的差异就失去了意义 —— 你分不清哪条差异是修复带来的。

---

## 7. 提交与交付

### 7.1 提交信息

中文，**说清「改了什么、为什么」**。一条 commit 讲一件事。

```
优化 db.js 的展平算法：O(n²) → O(n)

flattenTree 原来每个节点都 filter 两次找子项、再调一次 nodePath
（内部重建全量 id 表）。预建索引整趟共用、祖先路径顺着递归往下带。
实测 800 条待办：345ms → 3.4ms。行为不变（对拍 160/160）。
```

### 7.2 拆 commit

按「一组说得清的事」拆，不要一个巨型 commit。建议的切法：
**① 清理/死代码 → ② 重构 → ③ 行为修复 → ④ 样式 → ⑤ 测试脚手架 → ⑥ 文档**。
前面几组有后面的兜着，风险最低。

### 7.3 交付说明必须包含

1. **范围 / 目标 / 约束 / 交付形式**（先说清，再交代结果）
2. 逐文件、逐函数的改动说明（问题 → 改动 → 验证）
3. **实测前后对比**（有数字，不只说「更快了」）
4. 验证证据清单（每条写明方法与结果）
5. **风险、取舍与回滚**（逐项给回退命令）
6. **有意没做的 + 原因**（避免对方以为你漏了）
7. **自己引入又修掉的问题也要写**（诚实记录会让人更信任其他结论）

---

## 8. 本机环境（Windows）

### 8.1 路径

本机 **bash 不可靠**，脚本一律用绝对路径的运行时：

```
node    C:/Users/Administrator/.workbuddy/binaries/node/versions/22.22.2-3/node.exe
git     C:/Users/Administrator/.workbuddy/binaries/PortableGit/versions/1.2.0/cmd/git.exe
Edge    C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe
jsdom   C:/Users/Administrator/.workbuddy/binaries/node/workspace/node_modules
```

**没有 Chrome，也没有 playwright / puppeteer（不要装）。** 用 Edge，同为 Chromium 内核。

### 8.2 三个必踩的坑

| 坑 | 现象 | 解法 |
|---|---|---|
| **bash 沙箱** | `ls` / `head` / `tail` / `wc` 经常 `command not found`，管道整条失败**且无输出** | 一切文件统计用 Node 脚本，让脚本自己打印。**绝不用管道**（`node x.js \| tail -5` 会整条失败） |
| **`.git/refs/remotes` 写入被静默丢弃** | `git push` 报成功，但 `git status` 永远显示 `[gone]`、`git branch -r` 为空 | 沙箱只放行「已存在文件的修改」，新建文件被吞。用编辑器**直接写** `.git/refs/remotes/origin/main`（内容为完整 40 位 hash + 换行），**每次 push 后补写一次** |
| **同一文件的多个 Edit 不能并排发** | 后写的覆盖先写的，**两处都返回 success** | 一次只发一个 Edit，等结果再发下一个。改完用脚本 `indexOf` 核对关键字符串真的落盘 |

另外：`git clone` 走 HTTPS 会 `CONNECT tunnel failed` → 换 SSH。
本机 `git` 没有全局 `user.name` / `user.email` → 每个新仓库要单独 `git config` 一次
（本项目用 `Yu <wudimoyudawang@users.noreply.github.com>`，这样提交才归属账号）。

### 8.3 起服务看效果

**后台服务会和起它的那次调用一起结束。** 要「起服务 → 探活 → 跑驱动 → 收尾」，
必须在**同一次调用**里做完，否则第二次调用必然「连接被拒绝」。

---

## 9. 这些规则是怎么来的

规则不是凭空定的。出处：

| 规则 | 事故 / 实测 |
|---|---|
| 不许在循环里调单条查询 | `flattenTree` 是 O(n²)：800 条待办时 `todayTree` **345ms**；改成预建索引后 **3.4ms** |
| 派生值算一次挂到行对象 | 今日页一行 5 次全表扫描 = **11.6ms/行**，106 行 1.2 秒；改成读行字段后 **0.04ms/行** |
| 「没变化就跳过」要写在付代价之前 | `saveState` 每 2 秒全量序列化 = **45.7ms**，约 2.29% CPU 常驻；先判脏后 **0.003ms** |
| 重构不许改行为（含不修脏数据） | 对拍第一次跑就抓到「索引丢掉了空日期打卡记录」这处**我自己引入**的差异，老档案累计天数会变小 |
| 组件要用真实点击验，不能只看编译产物 | `FreqField.vue` 的 `@click="unit = u"` 绕过 `emit`：编译产物看着正常、选中态也高亮，但**存下去仍是「每天」**。jsdom 真实点击 6/9 → 修复后 9/9 |
| 纯样式改动要像素比对 | CSS 去重（197 → 148 个冗余块）后 10 张截图中 8 张有差异，逐像素定位后确认**全部是页头时钟那两个数字** |
| 断言要能证伪 | `build.cjs` 的自检探针串在源码里已不存在 → `!has()` 恒为真，那条断言**永远通过**，等于没有 |
| 批量替换脚本必须可核验 | ① toast 批量替换把 import 插进了**多行 import 中间** ② CSS 脚本忘推进 `selStart`，抽出的选择器是累积的，先删错了 7 个文件的 `.page`。两次都是**先回退到干净状态再重做**，没在坏状态上叠补丁 |
| 界面用语要单一出口 | `uni.showToast` 曾散在 **107 处**、直接调 `armDelete` **17 处**、「再点一次」文案 **10 份**、`armed` 状态各自维护 **16 处** |
| 行绑定样板要收敛 | `<TreeRow>` 的 9 行固定绑定被抄了 **6 遍** = 54 行纯样板，且 `today`/`domain` 各有 7 个同名同体的函数 |

---

## 10. 已知缺口（还没做的）

按优先级：

1. ~~**`importSnapshot` 的语义不一致**~~ —— **已解决（2026-09-23）**：导入改成整体替换，
   与界面文案一致（见 3.7 和 6.2）。对拍里补了 5 个捕获点钉住它。
2. **剩余 CSS 冗余**：还有约 148 个冗余规则块，其中多数是**近重复**（"`.block-h` 有 2~3 种变体），
   合并会改视觉，要留修饰类逐个处理。
3. **`test:component` 依赖 jsdom，而 jsdom 不在 `package.json` 里**。
   本项目刻意不引入这个依赖（装不上就 `SKIP` 并退出 0，不阻塞 `npm test`）。
4. **`test/lib/sandbox.cjs` 把依赖拍平到同一目录**。
   目前 store 只有 `db.js` + `seed.js`（同目录）够用，出现跨目录相对依赖时要改成保留目录结构。
5. **云端保存（远期，现在不做）** —— 用户 2026-09-23 明确：「后续可以加，但不是现在做」。
   它要解决的是跨设备一致性：导入/导出是手工的、一次性的，云端保存才是长久答案
   （也正因如此，导入做成「整体替换」是可以接受的 —— 档案的完整性由用户当时导出那一份负责）。
   **别把它当成本轮待办。** 真要做的时候记住 README 的第一条硬约束：
   核心层不许依赖网络 —— 所以它必须是可关闭的扩展层，关掉之后功能不减一分。

---

## 附：一次合格的改动长什么样

```
1. 读 AGENTS.md / PROMPT.md，确认要改的模块，找到它的数据层入口
2. npm test 取基线（记下捕获点数与结果）
3. 改一处（分成小步，每步可独立验证）
4. npm test                 → 差异逐条看清楚
5. 动视图/样式就补 build:h5 + test:e2e（样式还要像素比对）
6. 结论要有数字：从多少到多少，怎么量的
7. 写交付说明：范围/改动/证据/风险/回滚/有意未做的
8. 拆 commit 提交（不提交产物）
```
