# PROMPT · 新会话启动提示词

> **怎么用**：开一个新会话，把下面「提示词正文」整段贴进去（或者直接说一句
> **「读 `PROMPT.md`，然后按它说的做」**）。
>
> 这份文件是 `HANDOFF.md` 的**增量补丁**：HANDOFF 讲产品与设计的长期决策（那部分仍然有效），
> 这份讲**当前进度、原型做到哪了、下一步做什么、以及刚踩过的坑**。
> 两者冲突时以本文件为准，冲突点列在文末。
>
> 最后更新：**2026-09-19** · 对应原型 **v0.5**

---

## 提示词正文

我在继续开发「**书脊 · Spine**」这个项目（本地优先的个人计划中枢，仓库
<https://github.com/wudimoyudawang/spine>）。**先按下面顺序读，再动手。**

1. **`PROMPT.md`（本文件）** —— 现状与下一步，最新。
2. **`HANDOFF.md`** —— 产品定位、已定死的设计决策（D1–D5）、刻意不做的事、历史坑。
   **注意它有一处已过时**（见文末「HANDOFF 的过时点」）。
3. **`prototype/index.html`** —— 目前**唯一**的代码产物。单文件、零依赖、无构建、
   JS 渲染整页。它同时是设计说明和交互定稿，**先通读再改**。
4. **`docs/开工方案.md`** —— 真正开始写实现时的权威技术依据。

### 0. 怎么跟我协作（先读这段，能省一整轮返工）

- 我叫 **宇**，主语言 **Python**，是这个项目的开发者**也是唯一用户**。
  **直接给结论和取舍，不要长报告。**
- 我反复说过 **「先把页面搭建好，再开始具体实现」**（说过两次）。
  新功能**先出可点击原型**给我看结构和手感，我说 OK 再写实现。
- 我倾向「全都要」。我一次提多个需求时 **不要劝我砍功能**
  —— 上一个这么做的一整轮被我叫停了。正确做法是：
  ①把多个需求背后的统一抽象找出来，用一个机制覆盖掉；②给出先后顺序并说清代价。
- 界面上不要用 emoji 当图标；本机 IDE 是浅色主题，做视觉产物一律**浅底深字**。
- **需求落点不明确时先问再改**。同一句话往往有 2–3 个都说得通的落点，
  给它带**真实结果预览**的选项让我挑，比猜一次改一次省得多。

### 1. 项目是什么

一个**本地优先（local-first）的个人中枢**：习惯、待办、长短期计划、记账、热量与体重、
随心记、复盘全部收在一处。**未登录也能完整使用全部功能**，登录只为多端同步。

- 性质：**个人自用工具** + 小范围分享 + 开源（AGPL-3.0）。**不是商业产品。**
- 第一原则（也是验收标准）：**任何东西 3 秒内能记下来；今天该做什么，一眼看到。**
  量化为「记一笔」中位操作步数 **≤ 3 步 / ≤ 5 秒**。
- 四条数据/交互轴：统一事项模型（形态=习惯/待办/计划 · 领域 · 跨度 · 状态）、
  领域是主组织轴、形态是交互轴不进导航、跨度是属性不是页面。

### 2. 现在的进度

| 项 | 现状 |
|---|---|
| 阶段 | **设计定稿 + 原型迭代中，实现代码一行没写** |
| 仓库内容 | `docs/` 是设计文档，`prototype/index.html` 是唯一代码 |
| 原型版本 | **v0.5**（2026-09-19）。v0.4 → v0.5 累计 +537 行（1992 → 2529） |
| 数据 | **原型不落库**。数据都在页面内存的几个数组里：`DOMAINS` / `ITEMS` / `LOGS` / `TODAY_LOGS` / `RECORD_TYPES`，刷新即回初始值 |

> 原型不落库是**刻意的** —— 它的用途是让我看结构和手感。但由此产生了几个
> 「假交互」（写了界面没写逻辑），清单见第 4 节。

### 3. 原型已经做完的（v0.5）

**导航**：一级 7 项 —— 今日 / 收集 / 随心记 / 空间 / 记账 / 复盘 / 设置（设置带分隔线居末）。
**顶部快捷**：置顶的领域；**一个置顶都没有时整块隐藏**（连标题和分隔线一起消失）。

**今日页**
- 「今天 / 逾期」两块：待办列表，行尾常驻 **「×」删除**
- 「习惯」块：**从 `DOMAINS[].habits` 渲染**（v0.5 起；之前是写死的 3 条），行尾「打卡」+「×」
- 「计划」块：长期目标 + 进度条，行尾「×」；点条目打开编辑弹窗
- 「今天记下的」：操作日志（新增/修改/删掉）。**刻意不给删除入口**
- 三个新增入口在**各区块标题栏右侧**：`+ 新增待办` / `+ 新增习惯` / `+ 新增长期计划`

**新增弹窗（三个入口共用一套）**
- 填内容 + 说明，选「**放到哪个空间**」（胶囊一排，从 `DOMAINS` 生成，末尾带「+ 新建空间」）
- 默认选中当前所在空间；本轮**只走交互、不写数据**

**删除（v0.5 新增）**
- 覆盖今日页 4 类条目共 14 条（逾期待办 / 今日待办 / 习惯 / 长期计划）
- **两段确认**：第一下只「武装」（变红、文案变「确认删」），第二下才真删；
  4 秒无操作自动回退，Esc 立即解除，**全局同一时刻只允许一处待确认**
- 删除会真的落到 `ITEMS` / `DOMAINS`，并写一条 `op:'删掉'` 的今日记录

**其他已完成的交互**
- **首页速记**：一个输入框 + 类目 chips（自动判断/待办/随心记/记一笔支出/只丢进收件箱/
  热量摄入/热量消耗/体重/自定义），实时显示「会记成…」，认不出来进收件箱，
  任意页按 `/` 呼吁回首页并聚焦
- **快记自定义**：类目开关 + 规则表（关键词/正则，用户规则排在内置之前）+ **实时测试框**
- **长期计划进度控件**：滑杆 + `+1/+2/+5` 按钮，按钮优先仲裁（250ms 窗口），
  今日页与领域页两处百分比同源
- **长期计划弹窗**：草稿隔离，只有点「确认」才写回数据；取消/遮罩/Esc 直接丢弃
- **领域页**：习惯 + 待办 + 长期目标 + 记录项（记录项两种方式：`text` 纯文字 / `number` 数值）
- **空间页**：领域卡（☆ 置顶到顶部快捷、可新建领域）
- **记账页**：月度合计 + 分类柱状图（这两个是**真算的**，来自 `LOGS`）
- **设置页** 8 块：领域 / 快记类目与自动判断 / 记账分类 / 数据 /
  这一版不做的 / 规则引擎为什么只给关键词和正则 / 外观 / 技术栈

### 4. 待办（按建议优先级）

**A. 先把已有的「假交互」补成真的**（都在原型里，界面上看着能用、实际不通）

1. **记账数据通道是断的** —— 首页速记识别成「记一笔支出」时**只写 `TODAY_LOGS`，不写 `LOGS`**，
   所以月度合计和分类柱状图永远停在初始那 7 笔。**这是当前最该修的一条**，
   否则将来迁到 SQLite 时"写入路径不完整"会直接变成数据丢失。
2. **记账页的录入框是死的** —— `<input class="quick-in">` 没有 id、没有接线。
3. 今日页**没有金额摘要**（看不到"今天花了多少 / 本月花了多少"）。
4. 「收集 / 随心记 / 复盘」三页的多数按钮只弹 toast，未接逻辑。
5. **领域页（空间 → 点进某个领域）里的条目还没有删除入口** —— 今日页有，用同一套
   `delBtnEl` + 委托，接上只需在各渲染函数里加一行。
6. 16 处用 `★☆⚙` 之类的字符当图标（违反「不用 emoji 当图标」），集中在设置页与技术栈说明，
   属历史遗留。

**B. 然后才开始写实现**（按 `HANDOFF.md` 第 7 节）

第 1 步 = monorepo 骨架 + 数据模型 + `StorageAdapter` 抽象层 + 速记框跑通。
**不碰服务器、不碰 AI。** 第 1 步验收标准（我自己掐表）：
①「记一笔」中位 ≤ 3 步 / ≤ 5 秒；② 连续两周每天都打开过它。
**第 ② 条不达标时不要加新功能，先找摩擦。**

### 5. 关键上下文与踩过的坑

**架构层面**

- 原型是**单文件、零依赖、无构建**，所有交互靠挂在 `document` 上的**事件委托**
  （`e.target.closest('[data-x]')`）。改交互几乎不需要碰具体节点。
- **委托分支的顺序会决定行为。** 给一个已有可点击元素（例如计划行
  `.row-goal[data-goal-open]` 点开编辑弹窗）加内嵌子控件（例如删除「×」）时，
  新分支**必须排在外层分支之前**，否则点内层会触发外层。今天刚踩过，已加回归断言。
- **「只在某页显示」优先搭容器可见性。** 页面容器是
  `.page{display:none}` / `.page.is-on{display:block}`，控件放进页面里就自动跟着显隐 ——
  别写 JS 切换。我上一版为"按钮只在今日页出现"写了一整套 `is-off` 切换 + `go()` 钩子，
  下一版按钮移进页面后那套逻辑整套成了死代码，白写还白测。
- **窄屏断点是 920px**（`@media (max-width:920px)` 时 `.nav` 变横向滚动行）。
  用无头浏览器截图时**窗口宽度必须 ≥920px**，否则会截到另一种布局。

**交互层面**

- **「两端一致」= 触屏没有 hover。** 任何"hover 才出现"的控件在窄屏上等于不存在，
  这类设计一律改成**常驻**。
- **危险操作用「两段确认」**：第一下只武装（变色 + 换文案），第二下才执行，
  N 秒无操作自动回退，Esc 立即解除，**全局只允许一处处于武装态**（一个统一的
  `disarmDelete()` 一次清干净）。好处是命中区可以做小（26px）而误触无害 ——
  误触的代价只是"武装"，不是"执行"。正式版这里应走**墓碑软删除 + 撤销**
  （数据模型里已有 `deleted_at` 字段），原型不模拟。
- **重构纯函数要防语义漂移。** 原来写死的 `if` 改成规则表驱动后，
  「5 公里」一度被误判成 5 元支出 —— 排除式锚定的对象从"去掉数字后的剩余"变成了整句。
  这是重构纯函数最典型的隐蔽 bug，**没有反例单测会直接上线**。
- **反例单测比正例重要**：`交物业费 200`、`5 公里`、`今天热量控制得不错`、空串、`null`、
  写坏的正则 —— 这些才是真正拦住 bug 的用例。

**CSS 层面**

- **带 `border-radius` 的元素上只画单边线，线的两端一定会跟着圆角弯下去**
  （像一对钩子），而且**只在没有背景色时暴露**。改用绝对定位伪元素画 1px 直线，
  它不参与圆角计算。`border-left` / `border-right` 是同一个病。

### 6. 本机的验证方式（原型没有测试框架）

- **两层，都要跑**：**jsdom** 加载真实页面 + 派发真实事件做断言（验 DOM / 事件委托 /
  渲染出的文本），**无头 Chrome 截图**（验排版 / 间距 / 断点）。
  jsdom **不做 CSS 排版**，纯截图**不能驱动点击** —— 两层不可互相替代。
- 验证脚本在**仓库外**的 `D:\buddyproject\spine-analysis\verify-*.cjs`，
  目前 **5 套共 376 项**：`verify-nav`(73) / `verify-today-add`(96) / `verify-goal`(55) /
  `verify-goal-modal`(78) / `verify-today-delete`(74)。
- 跑法：`$env:NODE_PATH="C:\Users\17680\.workbuddy\binaries\node\workspace\node_modules"; node verify-xxx.cjs`
  （脚本自己 `writeFileSync` 写报告 txt，**不要再用 `*>` 重定向，会撞 EBUSY**）。
- 截图：本机有 Chrome（`C:\Program Files\Google\Chrome\Application\chrome.exe`），
  参数 `--headless=new --disable-gpu --hide-scrollbars --force-device-scale-factor=2 --window-size=1080,900 --virtual-time-budget=3000 --screenshot=out.png "file:///…"`。
  `--virtual-time-budget=3000` **必须加**（页面靠 JS 渲染，不加截到空壳）。
  本机**没有 playwright / puppeteer，也不要装**。
- **这 5 个脚本目前不在仓库里。** 换机或丢了就按上面两条重建：
  每次改完原型，**所有套件都要全跑一遍**（回归网的意义在全绿，不是单绿）。

### 7. 平台形态（尚未拍板）

- **手机端**：建议先 **PWA**；需要真 App 时用 **Capacitor** 打包，React 代码不用改。
- **PC 客户端**：建议 **Tauri 2**（Rust 外壳 + 系统 WebView2）+ 现有 React 前端 + 本地 SQLite。
  理由是体积 3–8MB、内存占用低，而 Electron 装完 100MB+、空跑吃两三百 MB ——
  对一个"随手记一笔"的工具是决定性的。前端 `dist/` 直接进打包，**几乎零迁移**。
  代价：Win10 1803 以下要装 WebView2（可打 fixed-version）、少量 Rust 学习成本、
  代码签名证书要花钱。
- 关键设计：**数据库藏在 IPC 后面，前端不直连 SQL**；数据访问层留一个 `Repository` 接口、
  两套实现（客户端 SQLite / 浏览器 IndexedDB），同一份前端既能当 PWA 也能进客户端。
- **这件事我没有明确表态，别当成已决定。**

---

## HANDOFF 的过时点（只有这几处，其余仍然有效）

| 位置 | HANDOFF 写的 | 实际 |
|---|---|---|
| 第 5 节「当前环境的验证方式」 | 「本机没有可用浏览器」，验证走静态校验 + 纯逻辑单测，原型跑过 **94 项**检查 | **已过时**。本机有 Chrome，已升级为 jsdom 驱动真实事件 + 无头截图，**5 套 376 项** |
| 第 3 节文件地图 | `prototype/index.html` 标注 **v0.4** | 现为 **v0.5** |
| 第 6 节第 4 条的坑清单 | 只列了 5 条 | 已补充：委托分支顺序、容器可见性、两端 hover、圆角单边线（见本文件第 5 节） |
| 第 7 节「下一步」 | 第 1 步 = 骨架 + 存储层 + 速记框 | **仍然有效，未变** |

HANDOFF 第 4 节的五条设计决策（D1–D5）、「刻意不做的事」清单、
第 8 节的用户偏好、第 9 节的三条原则 —— **全部仍然有效，不要推翻。**
