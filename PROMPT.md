# PROMPT · 新会话启动提示词

> **怎么用**：开一个新会话，把下面「提示词正文」整段贴进去（或者直接说一句
> **「读 `PROMPT.md`，然后按它说的做」**）。
>
> 这份文件是 `HANDOFF.md` 的**增量补丁**：HANDOFF 讲产品与设计的长期决策（那部分仍然有效），
> 这份讲**当前进度、原型做到哪了、下一步做什么、以及刚踩过的坑**。
> 两者冲突时以本文件为准，冲突点列在文末。
>
> 最后更新：**2026-09-20** · 对应原型 **v0.12**（只做手机端；数据落本机 + 可整份导出 JSON）

---

## 提示词正文

我在继续开发「**书脊 · Spine**」这个项目（本地优先的个人计划中枢，仓库
<https://github.com/wudimoyudawang/spine>）。**先按下面顺序读，再动手。**

1. **`PROMPT.md`（本文件）** —— 现状与下一步，最新。
2. **`HANDOFF.md`** —— 产品定位、已定死的设计决策（D1–D5）、刻意不做的事、历史坑。
   **注意它有几处已过时**（见文末「HANDOFF 的过时点」）。
3. **`prototype/index.html`** —— 目前**唯一**的代码产物。单文件、零依赖、无构建、
   JS 渲染整页。它同时是设计说明和交互定稿，**先通读再改**。
4. **`docs/开工方案.md`** —— 真正开始写实现时的权威技术依据。

### 0. 怎么跟我协作（先读这段，能省一整轮返工）

- 我叫 **宇**，主语言 **Python**，是这个项目的开发者**也是唯一用户**。
  **直接给结论和取舍，不要长报告。**
- 我反复说过 **「先把页面搭建好，再开始具体实现」**（说过两次）。
  新功能**先出可点击原型**给我看结构和手感，我说 OK 再写实现。
- 我倾向「全都要」。我一次提多个需求时 **不要劝我砍功能**
  —— 上一个这么做的一整轮被我叫停了。正确做法是：
  ①把多个需求背后的统一抽象找出来，用一个机制覆盖掉；②给出先后顺序并说清代价。
- 界面上不要用 emoji 当图标；本机 IDE 是浅色主题，做视觉产物一律**浅底深字**。
- **需求落点不明确时先问再改**。同一句话往往有 2–3 个都说得通的落点，
  给它带**真实结果预览**的选项让我挑，比猜一次改一次省得多。

### 1. 项目是什么

一个**本地优先（local-first）的个人中枢**：习惯、待办、长短期计划、记账、热量与体重、
随心记、复盘全部收在一处。**未登录也能完整使用全部功能**，登录只为多端同步。

- 性质：**个人自用工具** + 小范围分享 + 开源（AGPL-3.0）。**不是商业产品。**
- 第一原则（也是验收标准）：**任何东西 3 秒内能记下来；今天该做什么，一眼看到。**
  量化为「记一笔」中位操作步数 **≤ 3 步 / ≤ 5 秒**。
- 四条数据/交互轴：统一事项模型（形态=习惯/待办/计划 · 领域 · 跨度 · 状态）、
  领域是主组织轴、形态是交互轴不进导航、跨度是属性不是页面。

### 2. 现在的进度

| 项 | 现状 |
|---|---|
| 阶段 | **设计定稿 + 原型迭代中，实现代码一行没写** |
| 仓库内容 | `docs/` 是设计文档，`prototype/index.html` 是唯一代码。**曾经有一版 Windows 桌面版（Tauri 薄壳 + 便携版 exe），2026-09-20 按决定整个删掉了** |
| 原型版本 | **v0.12**（2026-09-20），`prototype/index.html` 现 **4893 行 / 234KB**。行数变化不是重点 —— **它换过三次结构**：v0.8 把「多级目录」换成「条目子项」，v0.10 起加**手机形态**（底部 Tab 栏 + 底部中间那颗「记一笔」），v0.12 起**数据落本机**。见下面两节 |
| 数据 | **落在本机**（`localStorage`，键 `spine.state.v1`）：关掉再打开，记的东西还在。内容就是页面内存里那些数组：`DOMAINS` / `ITEMS` / `LOGS` / `TODAY_LOGS` / `RECORD_TYPES` / `HABIT_LOGS` / `INBOX` / `NOTES` / `CATS`（记账品类清单），外加两份界面状态 `CURRENT`（停在哪一页）、`CAPTURE_MODE`（速记选了哪个模式）。设置页可整份导出 / 导入 JSON，**导出的文件只含数据、不含界面状态** |

> **v0.12 起数据落本机**，存储这一层刻意做薄：全部接口就是 `snapshot()` / `restore()` 两个函数，
> 调度也只有「改了就每 2 秒兜一次 + 页面隐藏/卸载时各一次」这一条路
> （不做「每个改数据的地方都记得调一次存」—— 那种写法迟早漏一处，而漏的永远是用户刚记的那一笔）。
> 将来换 SQLite 或接后端，改这两个函数即可。
> **存不下时必须出声**：`localStorage` 被禁或写满时，设置页那行会从「已自动保存」变成实情并弹一句提示。
> 用户以为存住了、其实没存，是这个应用最不能出的一种错。
> **v0.8 的主题是「子项」**：待办 / 习惯 / 计划三部分的**每一条都能往下挂子项，任意级**。
> v0.7 做的是「条目挂在目录下」，我看完的结论是**那个不是我要的** ——
> 我要的是**条目本身能拆**（一条待办拆成几条子待办），不是给条目造一个容器。
> 于是 `目录` 这个概念被整个拿掉，只剩 `条目.parent` 一个字段指向**另一条条目**。
> 并且按后面的要求收紧成：**子项只能建、不能改** —— 编辑弹窗里不留任何改「上级」的入口。
> 还剩哪些是假的，见第 4 节。

### 3. 原型已经做完的

**导航**：一级 7 项 —— 今日 / 收集 / 随心记 / 空间 / 记账 / 复盘 / 设置（设置带分隔线居末）。
**顶部快捷**：置顶的领域；**一个置顶都没有时整块隐藏**。

**手机形态（v0.9，同一份 `index.html` 自适应，没有另开文件）**
- 断点 **≤560px**：侧栏 `.side` 整块变**底部 Tab 栏**（fixed，54px 高，带 `env(safe-area-inset-bottom)`）。
  底部只留 5 个一级入口：今日 / 收集 / 随心记 / 空间 / 复盘。
- **记账 / 设置 收进「更多」抽屉**（`#moreBtn` + `#moreSheet`，含顶部快捷）。
  `#moreBtn` **故意不带 `data-go`** —— 带了就会被当成第 8 个页面。
  隐藏侧栏那两个要用 `.side>.nav>.navi[data-go=…]` 精确定位，用 `.nav .navi` 会把抽屉里的同名按钮一起藏掉。
- 切到记账 / 设置时，「更多」那颗替它们高亮（仍然由 `go()` 一处决定）。
- **断点按「宽度」判定，不是按「有没有触屏」** —— 手机上窄，桌面上把窗口拉窄到 560 以下
  也是同一套布局，只需要维护一份。
- **记东西的入口是底部中间那颗「记一笔」**（`#fabCap`，只有手机形态出现）。
  点开把速记框整块展开成**底部弹层**（同一份 DOM：#capture 加 `is-open` + `#capMask` +
  `body.cap-open`），不是第二份 —— 所以规则、品类胶囊、自定义面板全都自动共用。
  点它会先 `go('today')`（速记框长在今日页里），并顺带把「更多」抽屉收掉。
  记完**不自动关**，方便连着记几笔；关是点遮罩或 Esc。
  弹层开着时底部栏和那颗按钮都收起来（`body.cap-open .side, body.cap-open .fab{display:none}`），
  不会叠在弹层上。**`.main` 的底部留白是 130px**：要同时让开底栏（55）和浮在它上面的按钮（46+间距）。
- 触屏与安全区：`viewport-fit=cover`、`100dvh`、提示条抬到底部栏之上、点按目标放大
  （`.delbtn` 32 / Tab 54 / `.btn` 40）、`:active` 反馈、
  **输入框字号提到 16px**（小于 16px 时 iOS 一聚焦就放大整页）。
- ⚠️ **宇已明确：这个项目以后只做手机端，桌面版整个删掉了。**
  宽屏那套左侧栏布局还在（写都写了，成本为零），但**不要再为 PC 做新的设计决策**，
  也不要用「PC 上怎么办」当理由去否决一个手机端的做法。
- 手机端**不是从零做**，是接着 `≤920px` 那条改的。改之前先看那条和 ≤560 那条的先后关系。
- **那条 ≤560px 的 `@media` 必须放在样式表最后**：它和 ≤920px 那条会同时命中，
  CSS 里后出现的赢。原来写在前面，所以那半条基本是死的（老代码里的隐藏 bug，v0.9 修掉）。

**编辑（v0.7 起，今日页每一条都点得开）**
- **待办**：内容 / 到期日 / 领域 / **状态（待办·已完成）**
- **习惯**：习惯名 / 频率
- **计划**：走它自己的弹窗（那里有进度滑杆和 `+1/+2/+5`）
- 一个弹窗覆盖所有类型，**字段按类型生成**（`edFields()`），不是每种条目各写一个
- **草稿隔离**：改动先落在草稿上，点「保存」才写回数据；取消 / 点遮罩 / Esc = 整个丢弃。
  换选项重渲染前会先把输入框的值收回草稿，所以敲进去的字不会丢
- 「今天记下的」每一条也点得开：带 `ref` 的（那一笔支出、那条记录项）改的是**真实数据**；
  没 `ref` 的（新增 / 删除 / 归类这类操作流水）只给看和移除 —— **不假装能改历史**
- 顺带补上一个之前完全没有的入口：**把待办标成「已完成」**（`pickToday` 会跳过 done，
  但 v0.6 之前没有任何地方能把一条标成 done）
- **编辑弹窗里没有任何「上级」字段。** 子项关系只在创建时定，建完不给改；
  三种条目都如此。回归套件里有反向断言盯着（`#edFields [data-edsel="parent"]` 必须为 0、
  `#gmDirs` 必须不存在），谁再把上级选择器加回来会直接红。

**子项（v0.8 的主题，三部分共用一套）**
- 待办 / 习惯 / 长期目标里的**每一条**都能建子项，共用同一个 `renderTreeInto()`
  （唯一变化的是「每一行长什么样」的 `rowFn`）
- **没有「目录」这种东西**。只有 `条目.parent` 一个字段，指向**另一条同类条目**；
  任意级数都由它推出来，没有第二种结构要同步。
  一条待办的子项**还是一条待办**（能勾状态、能加说明、能再往下拆）
- 行尾那颗**虚线框的自绘加号**＝「在这条下面加一条子项」。
  点它**就地展开一行输入框**（不弹窗）—— 一条子项就是一句话，不值得为它弹一个窗。
  Enter 建好、Esc 收起、点别处也收起
- 有子项的条目左侧出现**折叠三角**；没有子项的只留占位（标题不会左右跳）
- **删掉一条时，它的子项上移一层接管它的位置**，不跟着一起删
  （「分类可以错，数据不该丢」这条对子项同样成立）
- 反馈行显示直接子项条数（`2 项`）；子项行左侧画一条 1px 竖线表达层级；
  子项那行还会写一句**上级路径**（`知识库项目上线 · 周三前`）—— 今日页不铺树，得知道它挂在谁下面。
  这是**只读展示**，不是入口
- **只能建，不能改。** 子项关系在创建那一刻定死，之后没有任何地方能改 ——
  编辑弹窗里没有上级字段（见上一节）。所以「防环」这件事天然成立，不需要额外守卫：
  新建时 `parent` 指向一条已存在的条目，而新条目还没有后代，成不了环
- 一次编辑可能同时牵动今日页 / 领域页 / 记账页 / 复盘页 → `refreshAfterEdit()` 全部重算
- **今日页也铺子项**（缩进一层，父项在前面）。「今天该做什么」仍然要一眼看到，
  所以子项只跟着父项出现，不单独按自己的日期冒出到顶层；
  同一张列表**只铺一次**，父项行和子项行不会各来一遍
- 今日页那套待办（`ITEMS`）**同样有子项**：`+` 写进 `ITEMS`，子项继承父项的领域和日期

> **v0.8 拿掉了什么**：`habitGroups` / `todoGroups` / `goalGroups` 三个数组、
> `dirRowEl()`、`newDirInline()`、`renameDirInline()`、`dirOpts()`、`CLOSED_DIRS`、
> `data-dir*` 一整套。目录改名换到编辑弹窗里 —— 改名本来就是改一条数据，不该有自己的入口。
> 回归套件里 v0.7 那一组（多级目录）随之作废并删除，别照着旧文档找它。
>
> **后来又把「改上级」也拿掉了**：`parentOpts()` / `itemParentOpts()` / `parentNameOf()` /
> `itemParentName()` / `subtreeIds()` / `itemsSubtreeIds()` / `renderGoalDirs()`、
> 弹窗里的 `#gmDirs` 字段与 `data-goalparent` 分支 —— 全删。
> 理由是需求本身：**子项只保留创建，创建后不提供任何修改入口**。
> 三个编辑弹窗里现在一个字都不提「上级」，回归套件换成反向断言盯着「不能有」。

**复盘「数值趋势」（v0.7 从写死三行改成可增删的清单）**
- `REVIEW_TRENDS` 只存「我选了哪几项」，每项怎么算由 `metricDefs()` 定义
- 每行带「×」删除（两段确认），下面「+ 添加一项」展开可选项
- 可选项 = 支出 / 打卡天数 / 待办完成 + **所有数值型记录项**（将来加「睡眠时长」
  「喝水量」会自动出现在这里，不用改代码）
- 每一项统一算「**本期 · 上一期**」—— 不再这周看均值、上周看最新值（那样横向比不是同一件事）
- 整数记录项的均值按整数显示（`1,823 kcal`，不是 `1,823.3 kcal`）
- **导出跟着这份清单走**：屏幕上没在看的项，不会出现在导出文本里

**打卡统计（v0.8 补的）**
- 两个数字：**连续打卡天数** / **累计打卡天数**。都从 `HABIT_LOGS` 长出来，
  唯一的计算入口是 `habitDates()` → `habitTotalDays()` / `habitStreakDays()` / `habitStat()`。
  **界面上任何地方都不许自己再数一遍** —— 数出两个不一致的数字比不显示更糟
- **累计 = 打过卡的不同日期数**，不是打卡次数。一天打三次只算一天
  （`toggleHabitLog` 本来就不会写第二条，`habitDates()` 再去一次重兜底）
- **连续 = 从今天往回数，遇到第一个空档就停**。
  关键契约：**今天还没打卡不算断** —— 这一天还没过完，早上打开就被判「断了」太伤人。
  所以今天有卡就从今天起数，今天没有就从**昨天**起数
- 显示在两处，措辞由 `streakText()` 一处生成（免得两处各说一个数）：
  - **习惯行尾部**（今日页 + 领域页）：`连续 3 天 · 累计 4 天`，连续 > 0 时数字用绿色强调。
    **一次都没打过卡的习惯不渲染这颗** —— 别拿两个 0 占位置
  - **复盘页「习惯打卡」表**：全部习惯都列（含没打过卡的，明说「还没有打卡记录」），
    三列 = 习惯 / 连续·累计 / **这一期 N 天**。换区间只影响「这一期」那一列，
    连续和累计是整天数口径、不跟着变。子习惯缩进一层，**不看折叠状态**（统计不该被折叠藏起来）
- 导出文本里逐条写：`训练日打卡：连续 3 天 · 累计 4 天（这一期 4 天）`；
  一次都没打过卡的不写（导出是能看的复述，不是数据转储）
- **打卡后整行重画**（`refreshAfterTick()`）：行里有统计，只换按钮文案会留下一颗过期的数字，
  「看着没错、其实是上一秒的真相」这种错最难发现
- 种子数据是**故意配过**的：四个习惯分别演示 连续 5 / 4 / 3 / 1 天，其中「阅读 30 分钟」
  09-16、09-18 空着（断过又续上）。只给清一色满勤的话，连续天数的算法写错了都看不出来

**今日页**
- 「今天 / 逾期」两块：待办列表（**带子项的缩进**），行尾常驻 **「×」删除**；点整行打开编辑弹窗
- 「习惯」块：从 `DOMAINS[].habits` 渲染（**带子项的缩进**），
  行尾 **「连续 N 天 · 累计 M 天」+「打卡」+「×」**；打过卡的行显示「已打卡」
- 「计划」块：长期目标 + 进度条（**带子项的缩进**），行尾「×」；点条目打开编辑弹窗
- **金额摘要一行**：速记框下方 `今天 ¥X · N 笔　本月 ¥Y`，右侧「去记账」
- 「今天记下的」：点得开（见上）；**刻意不给"改内容"**，只给"移除这一行"
- 三个新增入口在**各区块标题栏右侧**：`+ 新增待办` / `+ 新增习惯` / `+ 新增长期计划`
  （它们加的是**顶层**条目；加子项走行尾那颗加号）

**新增弹窗（三个入口共用一套）**
- 填内容 + 说明，选「放到哪个空间」（胶囊一排，末尾的「+ 新建空间」**就地**变输入框）
- 确认后真的写进 `DOMAINS`，并记一条「今天记下的」

**删除**
- 覆盖今日页 4 类条目、领域页的待办 / 习惯 / 长期目标、
  记账流水、随心记、收件箱、复盘的趋势项
- **删一条只把它自己拿掉，子项上移一层**
- **两段确认**：第一下只「武装」（变红、文案变「确认删」），第二下才真删；
  4 秒无操作自动回退，Esc 立即解除，**全局同一时刻只允许一处待确认**

**记账**
- 单一写入口 `addMoney()` —— 首页速记、记账页录入框都走它，
  所以「月度合计 / 分类柱状图 / 最近流水 / 今日摘要」四处同时更新
- 每一笔有稳定 `id`（v0.7），今日页那条流水能指回来改它
- 记账页录入框：`32 午餐` → 金额 32 + 分类「餐饮」（关键词猜，认不出记「未分类」）
- 「按项目看」**明确标成这一版不做** —— 不再显示一个假数字

**记账品类（v0.10）：记的时候能选，清单自己管**

> 起因是宇的一句「记账做一个单独的入口 或者说选中记账的时候可以选品类」。
> 问清楚之后落的点是：**导航不动 / 品类放速记框内联 / 可选 / 清单在设置页自己管**。

- **`CATS` 和 `CAT_WORDS` 是两件事，别合并**：`CATS` 是清单本身（顺序就是胶囊顺序，
  用户能加 / 改名 / 删）；`CAT_WORDS` 只是「自动猜」用的关键词，不进界面。
- 速记框选中「记一笔支出」→ `#captureCats` 出一排胶囊，第一颗是**「自动」**（= 不指定，
  照旧猜）。**不点的人一步都没多**，这是「记一笔 ≤3 步」预算能保住的前提。
- **手选赢过自动猜**：`addMoney(v,note,CAPTURE_CAT||r.category||guessCategory(...))`。
  「我明明点了餐饮」不能输给一条自动规则。
- **记完回到「自动」**，不记住上一次：选中态留着的话，下一笔忘了看就会被归到上一笔的类里，
  那种错在界面上看不出来，比不选更糟。（要它记住就改 `submitQuick` 里那一行。）
- 设置页的品类块：每行「名字 + 记过 N 笔 + 改名 + ×」，右上「+ 新建品类」。
  **新建和改名共用 `catInline()` 一个就地输入**（弹窗套弹窗是这个原型最容易走岔的地方）。
- **删除只从清单里拿走，不动已记的流水**（`category` 字符串原样留着，图表里还看得见）——
  「分类可以错，数据不该丢」。行上那个「记过 N 笔」就是让这句话不空。
- **改名要把已记的那几笔一起改**（`renameCategory()`），否则分类柱状图会裂成两根
  （旧名一根、新名一根），看起来就像数据坏了。
- `catList()` = `CATS` + 历史里出现过但已不在清单的（补在最后）。编辑弹窗用它，
  这样删过类之后仍然能把老记录改回原来的类。`catOpts()` 也改成走它。
- `renderCaptureTypes()` 里顺手调一次 `renderCaptureCats()` —— 两个容器永远同进同出，
  少一半「忘了在某个调用点加一行」的机会（调用它的地方有六处）。
- 用户自己能管的清单，所以它进了 `DATA_VARS` —— 接上本地存储之后要一起存下来。
  （`DATA_VARS` / `snapshot()` / `restore()` 是现成的落点，桌面壳删掉后暂时没有调用方。）

**收集 / 随心记**
- 收件箱真的落 `INBOX`，条数是真算的；每条右下的**「归类」就地展开面板**
  （今天待办 / 随心记 / 任意领域 / 先留着）
- 随心记「存下」真的落 `NOTES`，提示轮换，每条可删；**它不进任何统计与复盘**

**复盘**
- 周期三段：**本周 / 本月 / 自定义**（自定义给起止日期输入）
- 统计：待办完成 `done/total`、习惯有打卡的天数、训练次数
- **「写成新事项」**：把结论输入框里那句话真的变成今天的待办
- **「导出这一期」**：生成一段可直接全选复制的纯文本（不做静默下载）

**打卡**
- 习惯有自己的**稳定 id**；打卡写 `HABIT_LOGS`，再点一下撤销
- 今日页与领域页共用同一个行构建函数，两处状态不可能对不上
- 删除习惯时**连带清掉它的打卡记录**，不留孤儿数据

**其他**
- **首页速记**：输入框 + 类目 chips（自动判断/待办/随心记/记一笔支出/只丢进收件箱/各记录项），
  实时显示「会记成…」+ 命中了哪条规则，任意页按 `/` 呼吁回首页并聚焦
- **快记自定义**：类目开关 + 规则表（关键词/正则，用户规则排在内置之前）+ **实时测试框**
- **长期计划**：滑杆 + `+1/+2/+5`，按钮优先仲裁（250ms）；编辑弹窗草稿隔离。
  行布局分两层、按百分比定位（v0.8 末）：信息行 = 折叠三角 + `goal-main`(22%) + 滑杆 `gbar`(34%) + `goal-val`(9%) + `N 项` 徽标；
  操作行 = `goal-actions`(30%，`nowrap` 右对齐) 把子项「+」、`+1/+2/+5`、删除「×」**焊在同一行** ——
  窄屏整体换行，+ 和 × 永不分离
- **空间页 / 领域页 / 设置页 8 块**
- **图标**：`★☆⚙` 之类的字符图标**已清零**；星标 / 折叠三角 / 加号都是自绘 SVG
  （v0.8 那颗「加子项」的加号从文字 `+` 改成自绘 —— 它旁边就是计划行的 `+1/+2/+5`，
  两个文字加号挨着会长得一样）

### 3b. 界面文案（那些半透明的灰字）—— 有明确规矩

`.block-note` / `.note` / `.row-empty` / `.hint` / `.modal-hint` 这些都是 `--muted` 的灰字，
**它们是最容易写崩的地方**：写得像产品说明书，一眼就是机器写的。宇的原话是
「写的太多了，而且 AI 味道太重了」。现存的每一句都按下面这几条改过一遍：

- **一条只讲一件事，多数不超过 15 字。** 例：`永远置顶，不用往下翻` → `一直置顶`；
  `点卡片右上角的星标，把它放到左上角，随时一键进入` → `点星标，放进左上角快捷区`。
- **禁 `——`。** 界面文案里现在**一个破折号都没有**（代码注释里随便用）。
  破折号后面那些「解释为什么」的内容，九成可以直接删。
- **禁「不是…而是…」「不仅是…更是…」这类对仗**，也不要在末尾升华
  （「随时能带走，才是真的属于你」这种，改成「都在本机，随时能导出」）。
- **不解释设计意图，只讲不明显的事实和操作。** 用户不需要知道你为什么这么设计。
- **同一个意思不要在两处出现。** 今日页速记框下面已经有一条 `.capture-res`
  写着「认不出来也不会丢」，就别再压一条 `.note` 复述一遍 —— 两条灰字叠在一起
  是最典型的「AI 写多了」。
- **空态文案要给出下一步动作**，不要只描述状态。`还没有习惯。点「+ 新增习惯」加一个。`
- **喜报式、感叹式、安慰式的一律不要**（「轻松搞定」「无需担心」「就这么简单」）。
- 检查手法（改完跑一遍，应该输出 0）：
  ```bash
  node -e "const L=require('fs').readFileSync('prototype/index.html','utf8').split('\n');
  L.forEach((l,i)=>{if(l.indexOf('——')<0)return;const t=l.trim();
  if(/^(\/\/|\*|\/\*)/.test(t))return;
  if(/'[^']*——/.test(l)||/>[^<]*——/.test(l))console.log((i+1)+': '+t.slice(0,120));});"
  ```
- 灰字里**不要出现零宽字符**（U+200B）。已踩过一次：光标蹭进去一个，
  肉眼完全看不见，但会让文本匹配和后续编辑莫名其妙地失败。

### 4. 待办（按建议优先级）

**A. 还剩下的「假交互」和已知缺口**

1. **子项关系建完就锁死了** —— 只能在创建时定，之后既不能改「上级」、也不能拖拽。
   这是**刻意**的（宇明确要求只保留创建、不留任何编辑入口），但真实使用里
   「把这一整块挪到那边」迟早会发生 —— 到时候得先想清楚入口放哪，别又散到三个弹窗里。
2. **同级子项不能排序** —— 顺序就是数组顺序，新加的排最前（`unshift`）。也没有拖拽。
3. **父子状态不联动** —— 子项全做完了，父项不会自动标成完成；父项标完成也不管子项。
   刻意先不做（自动改状态会让人困惑），但**这是最可能被要求的下一条**：
   要做的话只做单向（子项全 done → 父项 done），别做双向。
4. 收件箱归类只有四个去向，**没有「给它加个日期」** —— 真实场景里这一步很常发生。
5. 复盘的自定义区间是两个裸 `date` 输入框，没做「上个月 / 最近 7 天」这类快捷。
6. 「按项目看」是这一版明确不做的（记账页已标注原因）—— 别当成漏了。
7. **打卡统计只做了「连续 / 累计」两个数。** 没做的：**历史最长连续**（这个挺自然，
   而且是「连续」最有价值的读法 —— 现在只能看到当下的连续，看不到自己的最好成绩）；
   近 N 天的**打卡小方格 / 热力条**；按周几看的规律（「周末最容易断」）。
   这些都是同一份 `HABIT_LOGS` 能算出来的，真要做时**只加显示，不要再加计算入口**。
   另外「连续」目前是**日历日**口径 —— 不跳周末、不按频率折算，所以
   「工作日」「每周 4 次」这类习惯，连到周末本来就该断，别当成 bug。
8. **`ITEMS` 和 `DOMAINS[].todos` 仍是两套待办数据**（今日页读前者，领域页读后者），
   这是历史包袱，也是 HANDOFF 里「统一事项模型」要解决的那件事。
   好消息是 **v0.8 之后两套用的是同一套子项机制**（都只是 `parent` 指向同类条目），
   合并时不用再迁一次结构。**尽快统一成一件事。**

**B. 然后才开始写实现**（按 `HANDOFF.md` 第 7 节）

第 1 步 = monorepo 骨架 + 数据模型 + `StorageAdapter` 抽象层 + 速记框跑通。
**不碰服务器、不碰 AI。** 第 1 步验收标准（我自己掐表）：
①「记一笔」中位 ≤ 3 步 / ≤ 5 秒；② 连续两周每天都打开过它。
**第 ② 条不达标时不要加新功能，先找摩擦。**

### 5. 关键上下文与踩过的坑

**架构层面**

- 原型是**单文件、零依赖、无构建**，所有交互靠挂在 `document` 上的**事件委托**
  （`e.target.closest('[data-x]')`）。改交互几乎不需要碰具体节点。
- **委托分支的顺序会决定行为。** 给一个已有可点击元素加内嵌子控件时，
  新分支**必须排在外层分支之前**，否则点内层会触发外层。已经踩过四次：
  计划行的「×」vs 打开弹窗、新增弹窗里的内联输入 vs 弹窗的 Enter、
  v0.7 的整行可编辑 vs 行内的「打卡」/「×」、
  **v0.8 的整行可编辑 vs 行内的「加子项」加号和折叠三角**。已加回归断言。
  v0.8 的实际顺序：`[data-del]` → `[data-tick]` → `[data-sub]` → `[data-fold]` →
  编辑弹窗那几个 → `[data-edit]` → 其余。
- **同一个东西只允许有一处构建它的代码。** 领域页的习惯行曾经漏了删除按钮，
  只因为「给待办传了 spec、给习惯忘了」。
  **v0.8 末尾发现这句话当时是假的**：实际上有 `habitRow()`（今日页）和 `habitRowEl()`（领域页）
  两份几乎一样的代码，唯一的差别是今日页那行多写一个领域名。
  文档写着「已统一」而代码没统一，比没写更坑 —— 加打卡统计时差点就要在两处各写一遍。
  已合并成一处：`habitRowEl(d,k,h,depth,showDomain)`，`habitRow()` 只是
  `habitRowEl(d,'habit',h,depth,true)`。**下次看到「唯一构建处」这句话，去代码里数一遍。**
- **三部分共用的功能就写成一份。** 「待办能建子项」不是给待办写的，
  是给 `parent` 字段写的：`renderTreeInto(box,d,k,rowFn,…)` 里唯一变化的是 `rowFn`。
  加第三部分不用再写一遍，也不用再测一遍。
- **列表项一律用稳定 id 定位，不要用数组下标。** 习惯原来是 `habit:领域id|下标`，
  删掉中间一个，后面全部错位。v0.7 把 `LOGS` 的每一笔、记录项的每一条记录也补了 `id`；
  v0.8 连 `DOMAINS[].todos` 和 `ITEMS` 的每一条也补了 —— **子项的 `parent` 只能指向 id**，
  没有 id 就做不了子项。
- **有副作用的写入只能有一个入口。** 记账能修好，不是补了某个分支，
  而是把所有入口收敛到 `addMoney()`。**「数据只写了一半的地方」比「完全不写」更难发现。**
- **弹窗里的改动先落草稿，确认才写回。** 长期计划弹窗和编辑弹窗都是这个形状，
  所以「取消」是真的什么都没发生。**重渲染字段前先把输入框的值收回草稿**
  —— 点一下「状态」胶囊就重建字段，不先收回，刚敲的字就没了。
- **加一条子项不要弹窗。** 弹窗套弹窗是原型最容易走岔的地方；
  而且「子项」天生是「一句话」，弹窗会逼着人填一堆不该填的字段。
  就地一行输入框（`openSubInline`）是这个项目里最快的一条路径。
  **它必须挡 Enter/Esc 冒泡**，外面还有整行点击和弹窗的键处理。
- **「只在某页显示」优先搭容器可见性。** `.page{display:none}` / `.page.is-on{display:block}`，
  控件放进页面里就自动跟着显隐 —— 别写 JS 切换。
- **弹窗里再放输入框，要挡住回车冒泡。** 弹窗本身监听 Enter = 确认，
  内联输入不 `stopPropagation` 的话，按一次回车会同时触发两件事。
- **窄屏断点是 920px**（`@media (max-width:920px)` 时 `.nav` 变横向滚动行）。
  用无头浏览器截图时**窗口宽度必须 ≥920px**，否则会截到另一种布局。

**交互层面**

- **「两端一致」= 触屏没有 hover。** 任何「hover 才出现」的控件在窄屏上等于不存在，
  这类设计一律改成**常驻**。
- **危险操作用「两段确认」**：第一下只武装（变色 + 换文案），第二下才执行，
  N 秒无操作自动回退，Esc 立即解除，**全局只允许一处处于武装态**。正式版这里应走
  **墓碑软删除 + 撤销**（数据模型里已有 `deleted_at` 字段），原型不模拟。
- **重构纯函数要防语义漂移。** 原来写死的 `if` 改成规则表驱动后，
  「5 公里」一度被误判成 5 元支出。**没有反例单测会直接上线。**
- **反例单测比正例重要**：`交物业费 200`、`5 公里`、金额为 0、空串、`null`、
  空标题、同一天重复打卡、折叠后再展开、点行内按钮误触整行 —— 这些才是真正拦住 bug 的用例。
- **写断言时先想清楚它会不会累积。** 有好几条期望值写错，都是因为忘了
  「前面的用例已经改过数据」。
- **断言要贴着真实契约。** v0.7 写错一条：「点打卡按钮 → 今天有打卡记录」——
  那条习惯**今天本来就打过卡**，点一下是**取消**。断言要断言「状态翻转」，不是「加上」。
- **v0.8 写错两条，都是「我以为的初始状态」和种子数据不一致**（那两条在测「改上级」，
  后来这个功能按宇的要求删了，但教训照样成立）：
  ① 以为某条待办是顶层的，其实种子里它已经挂在另一条下面 →
  于是「把它挂到 X 下面」这一步**等于没改动** → 不写记录 → 断言挂了。
  ② 断言「子项不该出现在它自己的上级选项里」时，挑的那条**根本没有子项** ——
  空集合上「没有一个元素满足 P」恒真，断言看着过了、其实什么都没验。
  **教训：写断言前先 `console.log` 一下真实初始数据，别凭记忆；别让断言在空集合上恒真。**
- **删掉一个功能时，把它的测试改写成「这个入口不该存在」的反向断言，别只是删掉。**
  v0.8 末尾去掉「改上级」时就是这么做的：原来是「选了上级 → parent 真的写进去了」，
  现在是「`#edFields [data-edsel="parent"]` 长度为 0、`#gmDirs` 不存在、
  整个字段区扫不到『上级』两个字、改别的字段后 parent 纹丝不动」。
  这样**删除本身也进了回归网** —— 谁哪天手滑加回来，CI 会红，而不是悄悄多出个入口。
- **断言「某条从列表里消失」要用 id，不要用文字。** 父项被标成 done 之后，
  它的**子项行里仍然带着父项的名字**（那是「上级路径」）—— 用文字查会误判成「还在」。
- **改了种子数据，就要把依赖计数的期望值一起更新。** v0.8 为了演示子项把种子数据加厚了一点
  （`HABIT_LOGS` 多两条、`ITEMS` 多两条子项），5 条「计数」断言的期望值随之更新。
  这不是「把测试改绿」，是**夹具变了**、期望值必须跟着变 —— 行为断言一条没动。
- **「今天是哪一天」要做成入参，不要读全局。** `habitStreakDays(id, today)` 而不是
  `habitStreakDays(id)` 自己去读 `TODAY` —— 否则「今天没打卡不算断」「连空两天才归零」
  这两个分支**根本测不到**（在真实今天上它们恰好只覆盖一种情况）。
  传进来之后就能站在 09-19、09-20、09-16 各断言一次。
  **凡是「相对于现在」的逻辑（连续、逾期、最近 N 天），都留一个时间的口子。**
- **同一个数字只允许有一处算法、一处措辞。** 「连续 3 天 · 累计 4 天」这句话在习惯行和
  复盘表里都要出现，就用 `streakText()` 生成 —— 两处各写一份模板，早晚会出现
  「一个说连续 3 天、一个说连续 2 天」。数字同理：行里、表里、导出里都读 `habitStat()`，
  **谁都不许自己再数一遍 `HABIT_LOGS`**。同一份数据给出两个不同的数，比不显示更糟。
- **「行重画」之后，测试里握着的节点引用会失效。** 给打卡加上统计后，点一下要重渲染整行
  （否则统计停在点之前的样子）。于是老测试里那句 `click(dom, tk)` 第二次点的是**已从文档里摘下来的旧节点** ——
  `dispatchEvent` 照发不误，但事件冒泡不到 `document`，**点了等于没点**，
  表现为「再点一下应该取消，结果没取消」。**重渲染之后必须重新取一遍元素**，
  这不是把测试改绿，是「行会被重画」这个行为变了、引用得跟着刷新。
- **过期的数字比没有数字更危险。** 打卡后只换按钮文案、不重算统计，界面上不会报错、
  数字看着也合理，只是**是上一秒的真相**。凡是「数据变了 + 别的地方显示了这个数据的派生值」，
  两处必须一起刷新 —— 这条和「单一写入口」「稳定 id」是同一类病。

**CSS 层面**

- **带 `border-radius` 的元素上只画单边线，线的两端一定会跟着圆角弯下去**
  （像一对钩子），而且**只在没有背景色时暴露**。改用绝对定位伪元素画 1px 直线。
  `border-left` / `border-right` 是同一个病 —— v0.7 目录里的那条竖线就是这么画的。
- **加 CSS 类之前先搜一遍这个类名。** 目录行本来想叫 `.grow`，而 `.flowrow .grow`
  已经被「占满剩余空间的 span」占了 —— **撞了不会报错**，只会静默套上 `flex:1`，
  然后你会以为是布局写错了。
- **删掉一个「撑排版」的元素前，先想它是不是在兼职做布局。** 区块头里
  `<tag> + <block-note 带 margin-left:auto> + <按钮>` 的右对齐，是靠**说明文字**撑出来的；
  单独删掉说明文字，按钮会紧贴标签。补一条 `.block-h>.tag+.addbtn{margin-left:auto}`
  只在「没有说明文字」时生效，别直接给按钮也加 `auto`（两个 auto 会平分空白，
  说明文字被挤到中间）。
- **负 margin 会吃掉邻居。** `.moneyline{margin:-8px 0 14px}` 是为了贴住上面那一条提示栏；
  在它**前面**插一个新元素，新元素会被这条负 margin 压住、和金额叠在一起。
  往一个「已经有负 margin 调过间距」的区域里塞东西时，插在后面，别插在前面。

**和编辑器一起工作（这一条很坑）**

- 宇会用图形编辑器直接打开 `index.html` 改文案。**编辑器保存时会把「当前激活的页面」写死进 HTML**
  —— 已经发生过一次：他把「空间 / 领域」页留在前台保存，于是
  `<nav>` 的高亮和 `<section class="page is-on">` 一起变成了「空间」，
  原型**从此打开就落在领域页**（页头 `#ptitle` 还留着「健身」）。
- 所以：**发现「落地页不对」时先看三处** —— `navi.is-on`、`section.page.is-on`、
  `#ptitle` / `#pdate` 的静态文本。它们必须都是「今日」。
- 根治办法已经加上了：脚本最后显式 `go('today')`。落地页、页头标题副标题、导航高亮
  三样现在都由 `go()` 一处决定，不再依赖 HTML 上那个静态 `is-on`。
  **以后往 boot 里加初始化，别把这一句挤掉。**
- 编辑器还会塞进 `style="position: relative;"`、`style=""` 之类的空壳属性，无害，不用管。
  但它有可能把**零宽字符**带进来（见 3b）。

### 6. 本机的验证方式（原型没有测试框架）

- **两层，都要跑**：**jsdom** 加载真实页面 + 派发真实事件做断言（验 DOM / 事件委托 /
  渲染出的文本），**无头浏览器截图**（验排版 / 间距 / 断点）。
  jsdom **不做 CSS 排版**，纯截图**不能驱动点击** —— 两层不可互相替代。
- **本机的实际环境（照这个来）**：
  - **本机没有 Chrome，也没有 playwright / puppeteer（不要装）**。用 **Edge**，
    同为 Chromium 内核、参数完全一样：
    `C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe`
  - 验证脚本在**仓库外**的 `C:\workbuddy\spine-analysis\`：
    - `verify-v06.cjs` —— **128 项**（v0.6 的全套，**保留不删**，它是老行为的回归网）
    - `verify-v08.cjs` —— **403 项** = v06 的 128 + v0.7 有效用例（编辑今日行 / 复盘趋势）
      + v0.8 子项一组（父子缩进 / 就地新增 / 三层 / 折叠 / 反例：空输入与 Esc / 删除抬升 /
      习惯子项 / 目标子项 / 今日页子项 / **反向断言：三种弹窗里都不许有改上级的入口**）
      + **打卡统计一组**（累计去重 / 连续断档 / 今天没打卡不算断 / 站在别的日期上再验一遍 /
      行内标签与高亮 / 没打卡就不显示 / 复盘表 / 换区间 / 导出文本 / 打卡后统计立刻跟上）
      + **手机端「更多」抽屉一组**（开合 / 跳页后自动收起 / Esc / 两处快捷一致 / 
      **反向断言：「更多」不许带 data-go、不许在抽屉里**）
      + **记账品类一组**（手选赢过自动猜 / 反例：写「打车」记成手选的餐饮 / 记完回到「自动」/
      新建 / 重名与空名挡住 / 改名连带已记的 / 删的两段确认 / **反向断言：删品类不动已记的** /
      删掉正选着的那个要落回「自动」/ 全删空的空态）
      + **`#captureRes` 一条反向断言**（v0.8.2 被宇主动删了，要恢复见 `gen-v08.cjs` 里那段说明）
    - `gen-v08.cjs` —— 生成器：`verify-v08 = verify-v06 + 新用例`。
      **改测试时改它、再重跑**，不要直接手改 verify-v08（下次生成会覆盖）
    - `mobile-geom.cjs` —— **手机形态的几何检查，72 项**（320 / 360 / 390 三个宽度）：
      不横向溢出、底部栏 fixed 贴底且够高、6 个 Tab 各 ≥44px、输入框无 <16px（iOS 不放大）。
      **jsdom 量不出这些，截图又进不了断言 —— 这是唯一能自动拦住手机布局回归的办法。**
      做法：起一个本地 HTTP 服务，页面里用 `new Image().src` 把测量值当查询串发回来
      （`--dump-dom` 在本机 Edge 上不出 stdout，file:// 又读不到父窗口）。
    - `shots.cjs` —— **21 张**截图 × **两档视口**（桌面 1080 / 手机 500）。
      **手机档必须跑**：`node shots.cjs --mobile`。
      **支持过滤参数**：`node shots.cjs --mobile today` 只截名字含该子串的用例
      （注意：截图只截**当前视口**，不是整页 —— 要截页面下方的块，得先在用例里 `scrollIntoView`）
      **四个坑（都踩实了，改脚本前先看它的头注释）**：png 是 Edge 进程退出后 10～16 秒才落盘，
      跑完立刻 `existsSync` 必然误报全挂；每张要独立 `--user-data-dir`，否则被「启动加速」的
      后台 Edge 接管、png 永远不来；`--window-size` 有约 490px 的布局宽度下限，390 拿不到；
      **iframe + position:fixed 截得到底色截不到字形**，别据此判断布局炸了。
  - jsdom 装在**托管 node 的隔离目录**里，不要全局装：
    `C:\Users\Administrator\.workbuddy\binaries\node\workspace\node_modules`
  - 跑法（绝对路径的 node + `NODE_PATH` 前置）：
    `cd C:/workbuddy/spine-analysis && NODE_PATH="C:/Users/Administrator/.workbuddy/binaries/node/workspace/node_modules" "C:/Users/Administrator/.workbuddy/binaries/node/versions/22.22.2-3/node.exe" verify-v08.cjs`
    脚本自己 `writeFileSync` 写报告 txt，**不要用 `*>` 重定向，会撞 EBUSY**。
  - 截图参数：`--headless=new --disable-gpu --hide-scrollbars --force-device-scale-factor=2
    --window-size=1080,1100 --virtual-time-budget=3000 --screenshot=out.png "file:///…"`
    `--virtual-time-budget=3000` **必须加**（页面靠 JS 渲染，不加截到空壳）。
  - **本机 bash 不可靠**：`head` / `tail` / `wc` / `ls` 之类经常找不到，别在管道里用；
    删文件用 Node 的 `fs.unlinkSync`。
- **jsdom 里有两类坑**：
  - `new W(dom).Event(...)` 会被解析成 `(new W(dom)).Event(...)`，报错位置落在点号上。
    先取到 `win` 再 `new win.Event(...)`。
  - **只读 `textarea` 的内容在 `.value` 里，`textContent` 永远是空的。** 读 textContent 会
    得到 `''`，断言写 `indexOf(...) >= 0` 就会莫名其妙地失败。
- **用脚本生成测试代码时，`String.replace` 的替换串里 `$$` 会被吃掉一个 `$`。**
  拼进去的 `$$(dom, …)` 会静默变成 `$(dom, …)`，报错却是「find is not a function」，
  离现场很远。替换串里有 `$` 就用函数形式：`s.replace(m, () => xxx)`。
- **这些脚本目前不在仓库里。** 换机或丢了就按上面重建：
  每次改完原型，**所有套件都要全跑一遍**（回归网的意义在全绿，不是单绿）。
  **建议下一步把它们提交进仓库（比如 `tools/`）—— 已经因为「不在仓库里」重建过一次了。**

### 7. 平台形态：**改用 uni-app 重构**（2026-09-20 宇拍板）

- **只做手机端。** PC 那一路（Tauri 薄壳）已经做的部分整个删掉了，别再把「PC 上怎么样」当成要解的问题。
- **2026-09-20 决定：用 uni-app 重写前端。** 两个理由：
  1. 手机版的手感和结构已经试出来了，**明显比 PC 版好**，说明这套结构值得落成真正的 App；
  2. 最终要装到手机上（APK）。uni-app 出包这条路最直接，还顺带能出小程序。
- ⚠️ **这推翻了先前「用 Capacitor、别用 uni-app」的评估结论。** 当时着眼的是
  「现有 HTML 零改动」，把「整页重写」当成了否决理由；宇着眼的是
  **要一个能长期做下去的移动端工程**，而不是给单文件原型续命。
  旧结论留在这里，只说明「这个选择是想过之后做的」——**别再拿它去劝**。
- **`prototype/index.html` 的角色变了**：从「唯一代码」变成**行为参照物**。
  里面定稿的东西（自动判断规则、打卡统计、品类清单、子项、存储与导出）是重构时
  **逐条对齐的验收口径**，不是可以随手丢的草稿。
  仓库外的回归网（`verify-v08.cjs` 472 项 / `mobile-geom.cjs` 72 项 / `shots.cjs`）在重构期间
  就是「原型行为」的基准，新工程要建立自己的等价物。
- 重构时要一并换掉的两处（原型上已经暴露）：
  - **存储**：现在 `localStorage` 顶着，接口只有 `snapshot()` / `restore()` 两个函数 → `uni.setStorageSync` 或 SQLite
  - **导出**：现在 `<a download>` + Blob URL，在 App 的 WebView 里不一定生效 → 走原生文件写入

**工程落点：`app/`（2026-09-21 起）**

```
app/
  package.json          uni-app Vue3 + Vite。全部 @dcloudio/* 锁同一个版本号
                        （3.0.0-5020620260917001），vite 锁 5.2.8 —— 这两条不能随手升
  src/pages.json        只有一个页面。见下面「为什么是单页」
  src/pages/index/      那唯一的页面：内部切 7 个视图
  src/views/            7 个视图。today 已搬完；其余 6 个是占位，每页单独一轮
  src/components/       TabBar（底部栏 + 「更多」抽屉）
  src/stores/seed.js    种子数据：脚本从原型原样抽出，日期已相对化
  src/stores/db.js      全部可变状态 + 存储 + 查询。对应原型的那些全局 var
  src/styles/base.scss  设计变量与全局样式（配色值和原型 :root 一致）
```

几个已经定下来的选择，动手前先看这里，别推翻重来：

- **单页 + 内部视图切换，不用 uni-app 的页面路由。** 原型本来就是「一个页面切 7 个视图」，
  这样底部栏（5 个常驻 + 中间浮起的「记一笔」+「更多」）能 1:1 还原。
  用内置 tabBar 的话第 6 项塞不下，还得改设计 —— 而宇明确说过他就喜欢现在这个样子。
- **状态用 Vue 的 `reactive`，不引 Pinia。** 这个应用的状态就是「一堆数组 + 读写它们的函数」，
  Pinia 的 devtools / 插件体系在这里没有收益，少一个依赖就少一个版本冲突点。
- **尺寸先用 px，不用 rpx。** 原型就是按手机宽度写死的 px，先让「长什么样」对上；
  rpx 化（大屏等比放大）等结构搬稳了单独做一次 —— 和搬结构混在一起改，
  出了问题就说不清是搬错了还是换算错了。
- **种子数据的日期会整体平移到真实今天。** 种子是围着 `2026-09-18` 配的，
  不平移的话新装上的人一进来看到的全是过期两周的待办，那不像工具，像坏掉的演示。
- **出 APK 绕不过 HBuilderX**：云打包要用它；离线打包也要它先「生成本地打包 App 资源」。
  本机没装，也没有 JDK / Android SDK。CLI 能开发、能在浏览器里预览，
  **最后出包那一步得在 HBuilderX 里点**。

- **导航结构改过（2026-09-21，宇定），和原型不一样，以这里为准**：
  - 底部栏常驻 **今日 / 收集 / 随心记 / 复盘** —— 「空间」不再占常驻位
  - **「更多」**抽屉里放 **空间 / 设置**
  - **「记一笔」**是底部栏最右那一颗实心胶囊。它不切页，是把今日页的速记框叫出来
    并把光标送进去（信号是 `captureAsk` 计数，见 stores/db.js）
  - **「记账」不再是独立入口**，它是「空间」里的一个**特殊空间**：
    卡片带「固定」标记，**没有置顶的星、没有改名、没有删除**。
    它**不进 `DOMAINS`**，是渲染时多出来的一张卡 —— 这样数据层不用为它开特例，
    因为记账的数据（LOGS / CATS）本来就不是领域的那套结构


---

## HANDOFF 的过时点（只有这几处，其余仍然有效）

| 位置 | HANDOFF 写的 | 实际 |
|---|---|---|
| 第 5 节「当前环境的验证方式」 | 「本机没有可用浏览器」，验证走静态校验 + 纯逻辑单测，原型跑过 **94 项**检查 | **已过时**。本机有无头浏览器（Edge），已升级为 jsdom 驱动真实事件 + 无头截图，**v0.8 为 312 项 + 17 张截图** |
| 第 3 节文件地图 | `prototype/index.html` 标注 **v0.4** | 现为 **v0.8** |
| 第 4 节「新版新增弹窗」 | 写的是「只走交互、不往 DOMAINS 写数据」 | **已过时**。v0.6 起三个入口都真的写数据 |
| 第 6 节第 4 条的坑清单 | 只列了 5 条 | 已补充：委托分支顺序、容器可见性、两端 hover、圆角单边线、稳定 id、弹窗回车冒泡、单一写入口、草稿隔离、类名冲突、`replace` 的 `$$`、就地加子项不弹窗（见本文件第 5 节） |
| 第 7 节「下一步」 | 第 1 步 = 骨架 + 存储层 + 速记框 | **仍然有效，未变** |

HANDOFF 第 4 节的五条设计决策（D1–D5）、「刻意不做的事」清单、
第 8 节的用户偏好、第 9 节的三条原则 —— **全部仍然有效，不要推翻。**
