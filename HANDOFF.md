# HANDOFF · 接力说明

**产品与设计决策读这一份。** 进度与下一步先读 `PROMPT.md`（它更新）。

> 这份文件是写给「下一个接手的人或 AI 助手」的。如果你是新接手的一方，
> 先读 `PROMPT.md` 知道现在在哪一步，再回来读本文的设计决策，
> 然后才去看 `docs/开工方案.md`。
>
> 最后更新：2026-09-18（**其中「当前阶段 / 下一步 / 文件地图」三处于 2026-09-21 跟着 `app/` 的进度改过**）

---

## 1. 三十秒速览

| 项 | 内容 |
|---|---|
| 项目 | **书脊 · Spine**（个人计划中枢）。仓库：<https://github.com/wudimoyudawang/spine> |
| 性质 | **个人自用工具**，小范围分享 + 开源。**不是商业产品** |
| 用户 | 单人（宇）。自己是主要用户，也是唯一开发者 |
| 当前阶段 | **原型已定稿（v0.12），uni-app 重构工程 `app/` 正在搬**：数据层 + 七个视图 + 速记面板已落地，规则表引擎等尾巴还没接完（现状见 `PROMPT.md` 第 2、7 节）。**方向已收敛为「只做手机端」**，曾做过的 Windows 桌面版已于 2026-09-20 删除 |
| 下一步 | 把 `app/` 的尾巴补完（`PROMPT.md` 第 4 节 **A0**：规则表引擎、收件箱归类、导出、回归网），再谈后端 |
| 技术栈 | 前端 **uni-app（Vue3 + UTS）—— 2026-09-20 定，见 `PROMPT.md` 第 7 节**；后端 Python+FastAPI+SQLModel+SQLite；Docker 单容器 |

**最重要的一句话**：这个项目**已经在「按商业产品流程做」上浪费过一整轮**。
不要再引入 PRD 评审、优先级评分卡、留存指标、路线图阶段门这类重型流程。
详见第 6 节第 1 条。

---

## 2. 一句话说清这个产品

> 一个本地优先（local-first）的个人中枢。习惯、待办、长短期计划、记账、热量与体重、
> 随心记、复盘全部收在一处。**未登录也能完整使用全部功能**，登录只是为了多端同步。

原始痛点不是「某个功能不够强」，而是**信息散在各处**（备忘录记事、日历排时间、Excel 记计划，
来回切换、容易漏）。

因此第一原则是验收标准而非口号：

> **任何东西 3 秒内能记下来；今天该做什么，一眼看到。**
> 具体为「记一笔」中位操作步数 ≤ 3 步 / ≤ 5 秒。

---

## 3. 文件地图（哪个是权威）

```
README.md                          项目门面
HANDOFF.md                         ★ 你正在读的这份：产品定位与长期设计决策
PROMPT.md                          ★ 本文的增量补丁：当前进度、下一步、刚踩的坑。**冲突以它为准**
CONTRIBUTING.md                    贡献约定（含「看起来像缺陷的刻意设计」清单）
LICENSE                            AGPL-3.0

docs/
  开工方案.md                       ★★ 唯一有效的开工依据。先读它
  平台形态与系统能力.md              ★ 手机/PC 形态选择 + 各平台能力边界
  与两个Agent项目的技术协同.md       远期参考。当前明确不做 AI，别提前动手
  架构图.svg                        技术架构图
  archive/                         已作废的过程文档，仅作决策留痕
    PRD-v1.2-2026-09-18.md
    roadmap-v1-2026-09-18.md
    roadmap-v2-2026-09-18.md
    research-01/02/03-*.md
    需求澄清-v0.1.md / .html

prototype/
  index.html                       ★ 单文件可交互原型（v0.12，只做手机端，数据落本机 + 可导出 JSON）。
                                   **2026-09-20 起重构后它的角色变了：行为参照物，不是唯一代码。**
app/                               ★ uni-app（Vue3 + Vite）重构工程 —— 当前的施工落点，
                                   工程结构与已定的技术选择见 `PROMPT.md` 第 7 节
```

**有效 / 作废的判断很简单**：`docs/` 根目录下的是有效文档；`docs/archive/` 下的一律作废。

`docs/archive/` 里那些文档是**用商业产品流程产出的**（PRD、两份路线图、三份调研报告）。
它们有参考价值（比如调研报告里的证据分级、竞品的功能聚合边界判断），
但**不构成任何必须完成的动作**。不要按它们的阶段划分施工 —— 见第 6 节。

---

## 4. 已定的设计决策（不要再讨论）

### 四条数据/交互轴

| # | 决策 | 含义 |
|---|---|---|
| D1 | **统一事项模型** | 所有事都是 `Item`，靠四项标签区分：**形态**（习惯/待办/计划）、**领域**（健身/学习/工作/个人…可自定义）、**跨度**（短期/长期）、**状态** |
| D2 | **领域是主组织轴** | 领域页**混装**该领域全部内容（用户明确要求「混装」，不要拆成多页） |
| D3 | **形态是交互轴，不进导航** | 习惯行=打卡按钮，待办行=勾选框，计划行=进度条 |
| D4 | **跨度是属性，不是页面** | 「短期/长期」不单独开页面（这是对用户原始需求的修正，已说明并接受） |
| D5 | **Local-first** | 本地是主体，云端可选。核心层不依赖网络/账号/外部服务 |

### 由 D1 推出的关键抽象（最容易做错的地方）

**「记账 / 热量 / 体重 / 打卡 / 领域记录项」不是五个功能，是同一种东西** ——
带数值的按时间记录。数据库里共用一张 `log` 表：

```
kind            TEXT   -- money | habit_tick | custom | …（注意：不要为每个新想法加内置枚举）
record_type_id  TEXT   -- 指向 record_type（领域自定义记录项时用）
occurred_at     INTEGER
value           REAL   -- 数值型（可为 NULL）
unit            TEXT
text            TEXT   -- ★ 纯文字型（可为 NULL）
category_id     TEXT
item_id         TEXT
payload         TEXT   -- JSON
```

**`value` 和 `text` 允许都为空、允许只有一个、也允许都有。** 这一条决定了
「纯文字记录」和「数值记录」不需要两张表，也决定了将来加「喝水量」「睡眠时长」
「阅读页数」「模拟考试分数」**都不用改表结构**。

> 反面教训：初版曾把 `calorie_in` / `weight` 写成 `kind` 的内置枚举。
> 已修正 —— 如果每个新想法都加一个内置类型，这个枚举迟早变成没人敢动的历史包袱。

### 领域与记录项（用户明确要求，别简化掉）

- 领域**用户可新建 / 改名 / 删除 / 置顶到顶部快捷**。每个领域天生带三样通用容器：
  **习惯 + 待办 + 长期目标**。新建领域不需要填任何额外东西就能用。
- **删除领域只删入口，不删内容**（内容回到收件箱等重新归类）。
  「分类可以错，数据不该丢」—— 否则用户以后就不敢删、也不敢建新领域。
- 领域里可自定义**记录项**，三字段：**名称 / 方式 / 单位**。方式**只有两种**：
  - `text` 纯文字 —— 记过程和感受（今天练了什么、错在哪）
  - `number` 数值 —— 看趋势（深蹲最大重量、模拟考试分数、读书页数），列表显示「最近/上次/增量」
- 移除记录项**只从界面收起**，已记内容保留在库和导出文件里。

> 为什么只给两种方式、不让用户自由设计表单字段：**字段一多录入就慢，录入一慢功能就死**。
> 记账和热量记录类功能的流失点全在这里。这是刻意的取舍，不要「优化」掉。

### 首页速记：只有一个输入框

- 一个框。写一句话 → 按规则判断类型 → 回车即存。**不跳页、不填表**。
- 类型 chips 常驻（自动/待办/支出/热量/体重/随心记/只丢进收件箱），手动选一次即覆盖自动判断。
- 输入框下方**实时显示「会记成：…」**，判断错了要能一眼看见并改掉。
- **认不出来进收件箱，不许丢、也不许瞎猜。** 保守优先：
  「交物业费 200」**不会**被记成 200 元支出（数字不在开头就不当金额），「5 公里」不会被当成 5 元。
  **宁可多一步归类，不能记错账。**
- 任何页面按 `/` 或 `Ctrl+K` 唤起（不在首页则先跳回并高亮输入框）。
- 记完的内容立刻在「今天记下的」区出现 —— 看得见才有正反馈。

### 自动判断规则可由用户编写

- 规则表驱动，四条内置规则**可见、可改、可关**（不是黑盒）。
- 每条规则：匹配什么 → 关键词/正则 → 排除什么 → 判成哪个类目（可判到任意记录项）。
- **用户的规则永远排在内置规则前面**，上移/下移**不许跨组**。
- **必须有实时测试框**：显示「会记成什么 + 命中哪条 + 其他每条为什么没命中」。
  没有它这个功能就是坏的 —— 用户写完不知道对不对，等于没写。
- 数值型目标但句子里没数字 → **放弃这条规则往下继续匹配**，都不中就进收件箱。不塞 0。
- **只给关键词和正则，不给脚本。** 理由：①允许执行用户写的 JS 等于把应用交给一段文本，
  这项目要开源、以后要让朋友注册，一条被导入的规则就能变成恶意代码；
  ②脚本只给结果不说明原因；③规则要跟数据一起导出换设备，脚本会把安全的事变危险。
  **不排斥以后支持脚本，但不在 v1 里留这个洞。**
- **已知风险：正则灾难性回溯。** JS 正则没有内置超时，`(a+)+` 这种会让页面假死。
  原型里有挡板（嵌套量词 + 超长直接拒存），但**真实实现必须再加「放进 Web Worker 设超时」**。
  这条是**上线前必做，不是优化项**（已写入 `docs/开工方案.md` 第 9 节）。

### 随心记 与 复盘（用户要求两者分开）

- **数据层共用、交互层分开**。
- 随心记 = **零字段入口**（只有文本框 + 时间戳）、无统计、**无连续天数、无红点、无提醒**。
- 随心记默认**不进任何聚合与复盘**，`note.review_visible` 默认 `0`，用户显式开启才打通。
  理由：知道会被翻出来看，写的时候就会开始表演。
- 复盘**不是数据实体，是「读取层 + 生成层」**：按周期（周/月/自定义）自动聚合打卡完成率、
  待办完成与延期、数值趋势，**并且结论必须能回写成新的 `Item`**（这是闭环关键）。
- 随心记的私密性要用**架构**保证（他人技术上不可读 / 本地加密），不是「默认私有」的软约束。

### 刻意不做的事（用户已同意，别当成待办）

| 不做 | 理由 |
|---|---|
| **streak 强惩罚**（漏了就断、清零视觉） | 证据一致表明这是打卡类功能被放弃的直接触发器。想加的人最可能主动要它，但一致反对 |
| **红点催促 / 提醒轰炸** | 同上。靠「打开就看到今天」，不靠焦虑驱动 |
| **红绿赤字框架**（支出超预算标红之类） | 制造负面情绪回避，是记账类功能被放弃的三大原因之一 |
| **强制完整性**（字段必填、分类必选） | 不完整但持续 > 完整但短暂 |
| **随心记与复盘合并** | 见上。方向相反：随心记是「往里扔」，复盘是「往外掏」 |
| 自建食物库、拍照识别份量作主线 | 录入摩擦最高的部分，不放在主线 |
| 社交 / 广告 / 付费墙 | 个人工具 |

---

## 5. 技术栈与关键约束

| 层 | 选型 | 何时 |
|---|---|---|
| 前端 | React + TypeScript + Vite + Tailwind（响应式 Web + PWA） | 第 1 步 |
| 前端存储 | IndexedDB（Dexie），**必须通过 `StorageAdapter` 抽象层访问** | 第 1 步 |
| 类型共享 | Pydantic → OpenAPI → 生成的 TS 类型 | 第 1 步定方案 |
| 后端 | Python + FastAPI + SQLModel | 第 5 步 |
| 数据库 | SQLite 单文件 | 第 5 步 |
| 部署 | Docker Compose 单容器 + Caddy | 第 5 步 |

### 三个必须提前知道的实操坑

1. **SQLite 文件绝不能放在 NAS 的 SMB/NFS 网络挂载目录上。** 网络文件系统的文件锁不可靠，
   会直接把库搞坏。放本地盘路径，靠导出/同步做跨机迁移。
2. **存储层必须抽象。** iOS 上浏览器存储可能被系统回收（见
   `docs/平台形态与系统能力.md`），将来要换 SQLite 时只应该改 `StorageAdapter` 这一层。
   第 1 步就把这层留出来，比事后重构便宜得多。
3. **开源前确认 `.gitignore` 覆盖 `.env` / `*.sqlite` / `data/`。** 密钥误入库是一票否决级事故，
   推上去 history 里永久留着。

### 为第二阶段（NAS 部署 + 朋友注册）预留的技术决策

这些必须**在第 1 步的数据模型里就留好**，否则将来做同步要推倒重来：

1. 主键用客户端生成的 **UUIDv7**（**禁止自增整型**）
2. **墓碑软删除**（`deleted_at`），不做物理删除
3. 每条记录带 **`rev` + `updated_at`**
4. **`device_id` / `owner_id` 占位字段**（现在不用，但 schema 必须有）
5. 随心记的**条目级加密边界**：密文 + nonce + `enc_version`，**密钥不与密文同存**，
   导出时默认排除明文
6. 统一 **UTC 时间戳**
7. **导出 schema 含上述同步字段，且支持幂等重入**

同步协议本身、CRDT、账号鉴权**全部推迟**。冲突策略将是**后写胜出 + 保留本地副本**，
**明确不做 CRDT**。

---

## 6. 已经踩过的坑（不要重犯）

### 1. 最大的一个：把个人工具当商业产品运营

初版走了完整的产品团队流程 —— 用户研究、竞品分析、数据分析、PRD、两份路线图、
五维加权优先级评分、北极星指标、14/30/60 天模块熔断阈值、利益相关者三视图……
用户本人叫停：**「这不是一个要上线的项目，是我个人要使用的，我只会做小范围的分享并开源。」**

**教训**：先判断「个人工具还是商业产品」，再决定要不要上重型流程。
规模判断错了，**流程越严谨越浪费**。

被砍掉的（别再引回来）：留存指标体系、五维加权评分表、利益相关者沟通文档、
模块熔断阈值表、7 阶段路线图、9 竞品对比矩阵。

保留下来的（因为和技术规模无关）：数据模型、记录流抽象、摩擦预算、同步预留字段、
Local-first 架构、Docker 与开源仓库结构。

### 2. 后端选型错过一次

初版后端定 Node + Fastify（理由是「前后端同一门语言」）。得知用户主语言是 **Python**、
且后续要做 agent 相关项目后改为 **Python / FastAPI**。

改动成本当时是零（因为第 5 步之前根本不碰后端）。**现在改比第 5 步改便宜得多。**

### 3. 职能写死过两次

- 健身的「力量训练 / 有氧」一开始写成**硬编码属性包** → 与「领域可自定义」矛盾，
  新建的领域里一个记录项都没有。已改成用户自定义记录项。
- 空间页一开始有「记录台」（记账/热量/体重三个写死卡片）→ 热量归到健身下之后才发现
  **记录台本身是多余的**。热量、体重就是「带单位的数字」，和「深蹲最大重量」没有任何结构区别。
  现在空间页只剩**记账**一个入口 —— 理由不是记账更重要，
  而是**它是唯一真需要独立页面的**（有收支方向、分类统计、按项目看）。

### 4. 原型迭代中的具体技术坑

写单文件 HTML 原型时踩到的，写在 `prototype/index.html` 里：

- **卡片内嵌按钮的事件委托顺序**：必须先判具体的 `[data-pin]` 再判外层 `[data-go]`，
  否则星标永远点不中。
- **二次确认不能用 `confirm()`**：预览是 iframe 会被拦。改成「再点一次确认」。
- **动态换卡时 `replaceChild` 的参数顺序**：`parent.replaceChild(新节点, 旧节点)`。
- **重构纯函数时的语义漂移**：把写死的 `if` 改成规则表驱动后，
  「5 公里」一度被误判成 5 元支出 —— 原来的排除式锚定在「去掉数字后的剩余」，
  改成规则引擎后变成了锚定**整句**。**这是重构纯函数最典型的隐蔽 bug，
  没有反例单测会直接上线。**
- **反例单测比正例重要**：`交物业费 200`、`5 公里`、`今天热量控制得不错`、空串、`null`、
  写坏的正则、排除式写错 —— 这些才是真正拦住 bug 的用例。

### 5. 当前环境的验证方式

如果本机没有可用浏览器（本机就是这种情况），单文件原型的验证走
**静态校验 + 纯逻辑单测**：

- Node 抽出 `<script>` 做语法编译
- JS 里 `getElementById('x')` 用到的每个 id 都在 HTML 中声明（反向也查）
- HTML/JS 里出现的每个 class 名在 `<style>` 里有定义
- 主要标签开闭配对
- 用极简 DOM stub **把整段脚本真跑起来**，对渲染结果做断言
  （这比只测纯函数强得多，面板展开后的按钮文案、列表内容、测试框输出都能测到）

`prototype/index.html` 当前版本（v0.4）跑过 **94 项检查，全部通过**。

---

## 7. 下一步要做什么

**第 1 步：仓库骨架 + 存储层 + 速记框跑通。**

> **2026-09-21 状态**：下面 1–3 项（骨架、存储抽象层、数据模型）已在 `app/` 里落了，
> 速记框也换成了「底部一颗加号 + 一个居中面板」。这一节剩下的价值是**验收标准**那两条，
> 不是待办清单。当前真正待办见 `PROMPT.md` 第 4 节。

具体目标：

1. 按 `docs/开工方案.md` 的仓库结构起 monorepo（前端 + `shared` 类型；后端目录可先留空）
2. 数据模型落地（`Item` / `log` / `record_type` / `domain` / `note` / `category` /
   `food_preset` / `review` / `insight`），**含第 5 节那 7 项同步预留字段**
3. `StorageAdapter` 抽象层 + IndexedDB 实现
4. 一个速记框 + 内置三条规则 + 列表
5. **跑起来，随手写一句话能存住并落对地方，刷新不丢**

**第 1 步不碰服务器、不碰 AI。**

**第 1 步的验收标准**（两条，用户自己掐表）：

1. 「记一笔」中位操作步数 ≤ 3 步 / ≤ 5 秒
2. 连续两周每天都打开过它

第 2 条不达标时**不要加新功能，先找摩擦**。

---

## 8. 尚未确定 / 需要用户拍板

| # | 问题 | 状态 |
|---|---|---|
| 1 | ~~项目名字~~ | ✅ **已定**：中文 **书脊**，英文 **Spine**（2026-09-18） |
| 2 | ~~推送到 GitHub 还是 Gitee~~ | ✅ **已定：GitHub** —— <https://github.com/wudimoyudawang/spine>，首次推送已完成 |
| 3 | ~~平台形态最终确认~~ | ✅ **已定（2026-09-20）：只做手机端。** PC 那一路整个删掉了。分析与边界见 `docs/平台形态与系统能力.md` |
| 4 | ~~git 提交邮箱~~ | ✅ 已改为 `wudimoyudawang@users.noreply.github.com`，提交归属账号（首次提交已 amend 修正） |
| 5 | 手机端是否要做成真 App | **先 PWA**（零工具链，加到主屏即可用）；确认需要硬件能力（蓝牙心率带等）或本地存储必须绝对可靠时，用 **Capacitor** 打包（页面代码不用改）。**注意：接手机壳之前得先接一层本地存储，否则关掉 App 数据就没了。** |

### 用户偏好（与协作方式有关，请遵守）

- 用户自称「**宇**」，中文交流，界面文案全中文。
- 他是**开发者本人**（主语言 Python），不是纯产品经理。技术上可以直接说结论和取舍。
- **他明确说「先把页面搭建好，再开始具体实现」**（说过两次）。新功能先出可点击原型给他看结构，
  他说 OK 再写实现。
- **他倾向「全都要」**。遇到他一次性提多个需求时：
  **不要劝他砍功能**（上一个这么做的一整轮被叫停了），
  而是 ①把多个需求背后的统一抽象找出来，用一个机制覆盖掉；②给出先后顺序，并说清代价。
- 他要**短的、能直接用的结论**，不要长报告。
- 本机 IDE 是 light 主题，**做视觉产物一律浅底深字**。

---

## 9. 长期事实速查

```
产品性质     个人自用 + 小范围分享 + 开源。不是商业产品。
主要用户     单人。第二阶段部署在自己的 NAS（内网穿透），朋友可注册
部署方式     Docker Compose 单容器
数据主权     Local-first，本地是主体；一键导出全量 JSON 防锁死
许可         AGPL-3.0
AI           当前明确不做。将来若做，只允许长在可一键关闭的扩展层，核心层零 LLM 调用
```

### 三个「如果拿不准，就按这个来」的原则

1. **拿不准要不要加这个功能** → 问「它能不能被今日视图或领域页吸收」。不能就不要。
2. **拿不准要不要加这个字段/步骤** → 问「它会不会让「记一笔」多一步」。会就不要。
3. **拿不准是个人工具还是商业产品** → **是个人工具。** 别上重型流程。
