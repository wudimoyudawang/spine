# 复制成 keystore.properties 并填上自己的值。
# 这个 .example 进仓库；真正的 keystore.properties 里是口令，**不进仓库**
# （根 .gitignore 拦了 shell/keystore.properties）。
#
# storeFile 可以写绝对路径，也可以写**相对路径**——相对路径按 shell/ 算。
# 最省事的做法：把 .jks 直接放进 shell/，然后 storeFile=spine-release.jks
# （根 .gitignore 拦了 *.jks，放这儿也不会被误提交。）
#
# 生成新 keystore（只在没有现成的时候用）：
#   keytool -genkeypair -v -keystore spine-release.jks -alias spine -keyalg RSA \
#           -keysize 2048 -validity 36500 -storepass <口令> -keypass <口令> \
#           -dname "CN=Spine, OU=Personal, O=Yu, L=Local, ST=Local, C=CN"
#
# ★ 这个 keystore 不要丢：丢了就打不出能覆盖安装的包了，只能卸载重装（数据一起没）。
#   换电脑时把它和这个 properties 一起带过去。

storeFile=spine-release.jks
storePassword=
keyAlias=spine
keyPassword=
